/* eslint-disable @typescript-eslint/no-explicit-any */
"use client";
import { FormEvent,useEffect,useState } from "react";
import { CheckCircle2,Film,Pause,Play,Search,Upload,XCircle } from "lucide-react";
import { adminSupabase,getSessionForScope } from "@/lib/supabase";
import { optimizeTrendingVideo } from "@/lib/videoUploadClient";
type Row=Record<string,any>;
async function headers(json=false){const session=await getSessionForScope("admin");if(!session)throw new Error("Your admin session has expired.");return{Authorization:`Bearer ${session.access_token}`,...(json?{"Content-Type":"application/json"}:{})}}
function localInput(value:string){const date=new Date(value);return new Date(date.getTime()-date.getTimezoneOffset()*60_000).toISOString().slice(0,16)}
export default function AdminTrendingCampaigns(){const[campaigns,setCampaigns]=useState<Row[]>([]);const[query,setQuery]=useState("");const[salons,setSalons]=useState<Row[]>([]);const[selectedSalon,setSelectedSalon]=useState<Row|null>(null);const[editing,setEditing]=useState<Row|null>(null);const[file,setFile]=useState<File|null>(null);const[notice,setNotice]=useState("");const[busy,setBusy]=useState(false);const[windowDefaults]=useState(()=>({start:localInput(new Date(Date.now()+3600000).toISOString()),end:localInput(new Date(Date.now()+8*86400000).toISOString())}));
async function load(){const response=await fetch("/api/admin/trending-campaigns",{headers:await headers(),cache:"no-store"});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to load campaigns.");setCampaigns(Array.isArray(body.campaigns)?body.campaigns:[])}
useEffect(()=>{const timer=window.setTimeout(()=>void load().catch((error)=>setNotice(error instanceof Error?error.message:"Unable to load campaigns.")),0);return()=>window.clearTimeout(timer)},[]);
useEffect(()=>{if(query.trim().length<2||editing){const timer=window.setTimeout(()=>setSalons([]),0);return()=>window.clearTimeout(timer)}const controller=new AbortController();const timer=window.setTimeout(()=>void(async()=>{const response=await fetch(`/api/admin/trending-campaigns?mode=salons&q=${encodeURIComponent(query)}`,{headers:await headers(),signal:controller.signal});const body=await response.json();if(response.ok)setSalons(body.salons||[])})().catch((error)=>{if(error.name!=="AbortError")console.error(error)}),220);return()=>{window.clearTimeout(timer);controller.abort()}},[editing,query]);
async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const salonId=editing?.salon_id||selectedSalon?.id;if(!salonId){setNotice("Search for and select an eligible salon.");return}if(!editing&&!file){setNotice("Choose a video to upload.");return}setBusy(true);setNotice("Validating and optimizing video…");let uploadedPath="";try{let video={video_url:editing?.video_url,storage_path:editing?.storage_path,duration_seconds:editing?.duration_seconds,file_size_bytes:editing?.file_size_bytes,mime_type:editing?.mime_type};if(file){const optimized=await optimizeTrendingVideo(file);uploadedPath=`campaigns/${salonId}/${Date.now()}-${crypto.randomUUID()}.${optimized.file.type==="video/webm"?"webm":"mp4"}`;const{error}=await adminSupabase.storage.from("trending-videos").upload(uploadedPath,optimized.file,{cacheControl:"31536000",contentType:optimized.file.type,upsert:false});if(error)throw error;const{data}=adminSupabase.storage.from("trending-videos").getPublicUrl(uploadedPath);video={video_url:data.publicUrl,storage_path:uploadedPath,duration_seconds:optimized.duration,file_size_bytes:optimized.file.size,mime_type:optimized.file.type}}const payload={action:"save",id:editing?.id||null,salon_id:salonId,...video,thumbnail_url:editing?.thumbnail_url||null,description:form.get("description"),status:form.get("status"),starts_at:form.get("starts_at"),ends_at:form.get("ends_at"),timezone:form.get("timezone"),radius_miles:form.get("radius"),priority:form.get("priority"),rotation_weight:form.get("weight"),internal_note:form.get("note"),entitlement_source:form.get("entitlement_source"),entitlement_reference:form.get("entitlement_reference"),entitlement_amount_minor:form.get("amount")?Math.round(Number(form.get("amount"))*100):null,reason:form.get("reason")};const response=await fetch("/api/admin/trending-campaigns",{method:"POST",headers:await headers(true),body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to save campaign.");if(file&&editing?.storage_path&&editing.storage_path!==uploadedPath)await adminSupabase.storage.from("trending-videos").remove([editing.storage_path]);await load();setEditing(null);setSelectedSalon(null);setQuery("");setFile(null);event.currentTarget.reset();setNotice("Trending campaign saved. New or replaced videos require moderation approval.")}catch(error){if(uploadedPath)await adminSupabase.storage.from("trending-videos").remove([uploadedPath]);setNotice(error instanceof Error?error.message:"Unable to save campaign.")}finally{setBusy(false)}}
async function moderate(campaign:Row,decision:string){const reason=window.prompt(`${decision} reason:`)?.trim()||"";if(reason.length<5){setNotice("Enter a moderation reason of at least 5 characters.");return}setBusy(true);try{const response=await fetch("/api/admin/trending-campaigns",{method:"POST",headers:await headers(true),body:JSON.stringify({action:"moderate",id:campaign.id,decision,reason})});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to moderate video.");await load();setNotice(`Video ${decision.toLowerCase()} and audit recorded.`)}catch(error){setNotice(error instanceof Error?error.message:"Unable to moderate video.")}finally{setBusy(false)}}
async function status(campaign:Row,next:string){const reason=window.prompt(`Reason for ${next.toLowerCase()}:`)?.trim()||"";if(reason.length<5){setNotice("Enter a reason of at least 5 characters.");return}const payload={action:"save",id:campaign.id,salon_id:campaign.salon_id,video_url:campaign.video_url,storage_path:campaign.storage_path,thumbnail_url:campaign.thumbnail_url,description:campaign.description,duration_seconds:campaign.duration_seconds,file_size_bytes:campaign.file_size_bytes,mime_type:campaign.mime_type,status:next,starts_at:campaign.starts_at,ends_at:campaign.ends_at,timezone:campaign.timezone,radius_miles:campaign.radius_miles,priority:campaign.priority,rotation_weight:campaign.rotation_weight,internal_note:campaign.internal_note,reason};setBusy(true);try{const response=await fetch("/api/admin/trending-campaigns",{method:"POST",headers:await headers(true),body:JSON.stringify(payload)});const body=await response.json();if(!response.ok)throw new Error(body.error||"Unable to change status.");await load();setNotice(`Campaign ${next.toLowerCase()}.`)}catch(error){setNotice(error instanceof Error?error.message:"Unable to change status.")}finally{setBusy(false)}}
return <div className="space-y-5">{notice?<p role="status" className="rounded-lg border border-magenta/20 bg-blush/50 p-3 text-xs text-plum">{notice}</p>:null}<section className="rounded-[15px] border border-plum/10 bg-white p-5"><div className="flex items-center gap-3"><Film className="text-magenta"/><div><h2 className="font-serif text-2xl text-plum">Trending Picks campaigns</h2><p className="text-xs text-ink/55">Upload a short salon video, moderate it, attach a paid entitlement, then schedule local radius placement.</p></div></div><form onSubmit={submit} className="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><div className="relative sm:col-span-2"><Label text="Eligible salon"><div className="relative"><Search className="absolute left-3 top-3.5 text-ink/40" size={15}/><input disabled={Boolean(editing)} value={editing?.salon?.name||query} onChange={(event)=>{setQuery(event.target.value);setSelectedSalon(null)}} className="min-h-11 w-full rounded-lg border border-plum/15 pl-9 text-xs" placeholder="Search salons"/></div></Label>{salons.length&&!selectedSalon?<div className="absolute z-20 mt-1 w-full rounded-lg border bg-white p-1 shadow-xl">{salons.map((salon)=><button type="button" key={salon.id} onClick={()=>{setSelectedSalon(salon);setQuery(salon.name);setSalons([])}} className="block w-full rounded p-3 text-left text-xs hover:bg-blush"><b>{salon.name}</b> · {salon.address_city}, {salon.address_state}</button>)}</div>:null}</div><Label text={editing?"Replacement video (optional; resets moderation)":"Video (MP4/WebM, ≤30 sec)"}><input type="file" accept="video/mp4,video/webm" required={!editing} onChange={(event)=>setFile(event.target.files?.[0]||null)} className="min-h-11 w-full rounded-lg border p-2 text-xs"/></Label><Field name="description" label="Description" defaultValue={editing?.description}/><Field name="starts_at" label="Start" type="datetime-local" defaultValue={editing?localInput(editing.starts_at):windowDefaults.start}/><Field name="ends_at" label="End" type="datetime-local" defaultValue={editing?localInput(editing.ends_at):windowDefaults.end}/><Field name="timezone" label="Timezone" defaultValue={editing?.timezone||Intl.DateTimeFormat().resolvedOptions().timeZone}/><Select name="status" label="Status" defaultValue={editing?.status||"Draft"} options={["Draft","Scheduled","Active","Paused","Expired"]}/><Field name="radius" label="Radius miles" type="number" min="1" max="250" defaultValue={editing?.radius_miles||25}/><Field name="priority" label="Priority" type="number" min="0" max="100" defaultValue={editing?.priority??50}/><Field name="weight" label="Rotation weight" type="number" min="0.1" max="100" step="0.1" defaultValue={editing?.rotation_weight||1}/><Select name="entitlement_source" label="Entitlement" defaultValue="" options={["","stripe_payment","verified_invoice","platform_credit"]}/><Field name="entitlement_reference" label="Payment / credit reference" placeholder={editing?.entitlement?.external_reference||"Reference ID"}/><Field name="amount" label="Amount USD" type="number" min="0" step="0.01"/><Field name="note" label="Internal note" defaultValue={editing?.internal_note}/>{editing?<Field name="reason" label="Change reason" required/>:null}<div className="flex items-end gap-2 xl:col-span-2"><button disabled={busy} className="inline-flex min-h-11 flex-1 items-center justify-center gap-2 rounded-lg bg-magenta px-5 text-xs font-bold text-white"><Upload size={14}/>{busy?"Saving…":editing?"Save audited changes":"Upload draft campaign"}</button>{editing?<button type="button" onClick={()=>{setEditing(null);setFile(null)}} className="min-h-11 rounded-lg border px-4 text-xs font-bold">Cancel</button>:null}</div></form></section><section className="grid gap-4 lg:grid-cols-2">{campaigns.map((campaign)=><article key={campaign.id} className="overflow-hidden rounded-[15px] border border-plum/10 bg-white"><video src={campaign.video_url} controls preload="metadata" poster={campaign.thumbnail_url||undefined} className="aspect-video w-full bg-ink object-cover"/><div className="p-4"><div className="flex flex-wrap items-center gap-2"><h3 className="font-serif text-lg text-plum">{campaign.salon?.name}</h3><Badge value={campaign.status}/><Badge value={campaign.moderation_status}/></div><p className="mt-2 text-xs text-ink/65">{campaign.description}</p><p className="mt-2 text-[10px] text-ink/50">{campaign.radius_miles} mi · priority {campaign.priority} · {new Date(campaign.starts_at).toLocaleString()} → {new Date(campaign.ends_at).toLocaleString()}</p><div className="mt-3 flex flex-wrap gap-2"><button onClick={()=>setEditing(campaign)} className="min-h-10 rounded-lg border border-magenta px-3 text-[10px] font-bold text-magenta">Edit</button>{campaign.moderation_status!=="Approved"?<button disabled={busy} onClick={()=>void moderate(campaign,"Approved")} className="inline-flex min-h-10 items-center gap-1 rounded-lg bg-green-700 px-3 text-[10px] font-bold text-white"><CheckCircle2 size={13}/>Approve</button>:null}{campaign.moderation_status!=="Rejected"?<button disabled={busy} onClick={()=>void moderate(campaign,"Rejected")} className="inline-flex min-h-10 items-center gap-1 rounded-lg border border-red-300 px-3 text-[10px] font-bold text-red-700"><XCircle size={13}/>Reject</button>:null}{campaign.status==="Active"?<button onClick={()=>void status(campaign,"Paused")} className="inline-flex min-h-10 items-center gap-1 rounded-lg border px-3 text-[10px] font-bold"><Pause size={13}/>Pause</button>:campaign.status==="Paused"?<button onClick={()=>void status(campaign,"Active")} className="inline-flex min-h-10 items-center gap-1 rounded-lg bg-plum px-3 text-[10px] font-bold text-white"><Play size={13}/>Resume</button>:null}</div>{campaign.audit?.length?<details className="mt-3 text-[10px]"><summary className="font-bold text-magenta">Audit history ({campaign.audit.length})</summary>{[...campaign.audit].sort((a:Row,b:Row)=>String(b.created_at).localeCompare(String(a.created_at))).map((entry:Row)=><p key={entry.id} className="mt-2 border-l-2 border-magenta pl-2"><b>{entry.action}</b> · {entry.reason||"Initial creation"}</p>)}</details>:null}</div></article>)}{!campaigns.length?<p className="rounded-[15px] bg-white p-10 text-center text-xs text-ink/55 lg:col-span-2">No Trending Picks campaigns yet.</p>:null}</section></div>}
function Label({text,children}:{text:string;children:React.ReactNode}){return <label className="block text-[10px] font-bold">{text}<span className="mt-1 block">{children}</span></label>}
function Field({name,label,type="text",defaultValue,placeholder,required=false,min,max,step}:{name:string;label:string;type?:string;defaultValue?:string|number;placeholder?:string;required?:boolean;min?:string;max?:string;step?:string}){return <Label text={label}><input key={`${name}-${defaultValue}`} name={name} type={type} required={required} defaultValue={defaultValue} placeholder={placeholder} min={min} max={max} step={step} className="min-h-11 w-full rounded-lg border border-plum/15 px-3 text-xs font-normal"/></Label>}
function Select({name,label,defaultValue,options}:{name:string;label:string;defaultValue:string;options:string[]}){return <Label text={label}><select key={`${name}-${defaultValue}`} name={name} defaultValue={defaultValue} className="min-h-11 w-full rounded-lg border border-plum/15 bg-white px-3 text-xs font-normal">{options.map((option)=><option value={option} key={option}>{option||"Attach later"}</option>)}</select></Label>}
function Badge({value}:{value:string}){return <span className="rounded-full bg-blush px-2 py-1 text-[9px] font-bold text-plum">{value}</span>}
