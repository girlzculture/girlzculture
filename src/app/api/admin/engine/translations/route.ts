import { requireAdminPermission } from "@/lib/supabaseAdmin";
import { ENGLISH_MESSAGES, SUPPORTED_LOCALES } from "@/i18n/catalog";
const HIGH_IMPACT=new Set(["booking","billing","security","safety","legal"]);
export async function GET(request:Request){try{const{admin}=await requireAdminPermission(request,"content");const[{data:locales,error:localeError},{data:stored,error:entryError},{data:content,error:contentError}]=await Promise.all([admin.from("supported_locales").select("*").order("sort_order"),admin.from("translation_entries").select("*").order("namespace").order("translation_key"),admin.from("localized_content").select("*").order("entity_type").limit(500)]);if(localeError)throw localeError;if(entryError)throw entryError;if(contentError)throw contentError;const index=new Map((stored||[]).map(row=>[`${row.translation_key}:${row.locale}`,row]));const entries=SUPPORTED_LOCALES.flatMap(locale=>Object.entries(ENGLISH_MESSAGES).map(([key,source])=>index.get(`${key}:${locale}`)||{id:"",translation_key:key,locale,namespace:key.split(".")[0],source_text:source,translated_text:locale==="en"?source:"",status:locale==="en"?"Published":"Missing",impact_level:/login|signup/.test(key)?"security":"standard",version:0}));return Response.json({locales:locales||[],entries,content:content||[]})}catch(error){console.error("Translation manager load failed",error);return Response.json({error:error instanceof Error?error.message:"Unable to load translations."},{status:403})}}
export async function PATCH(request:Request){try{const{admin,user}=await requireAdminPermission(request,"content");const body=await request.json() as Record<string,unknown>;const id=String(body.id||"");const action=String(body.action||"save_draft");const expectedVersion=Number(body.version||0);const text=String(body.translated_text||"").trim();if(!text)throw new Error("Translation text is required.");let existing:Record<string,unknown>;if(id){const{data,error}=await admin.from("translation_entries").select("*").eq("id",id).single();if(error)throw error;existing=data;if(existing.version!==expectedVersion)throw new Error("Another administrator updated this translation. Reload before saving.")}else{const key=String(body.translation_key||"");const locale=String(body.locale||"");if(!(key in ENGLISH_MESSAGES)||!SUPPORTED_LOCALES.includes(locale as typeof SUPPORTED_LOCALES[number]))throw new Error("Choose a valid translation entry.");existing={translation_key:key,locale,source_text:ENGLISH_MESSAGES[key],translated_text:"",impact_level:String(body.impact_level||"standard"),namespace:key.split(".")[0],version:0}}
let status="Draft";const update:Record<string,unknown>={translated_text:text,status,updated_by:user.id,machine_generated:Boolean(body.machine_generated)};if(action==="publish"){if(HIGH_IMPACT.has(String(existing.impact_level))&&!body.confirm_review)throw new Error("Legal, payment, safety, security, and booking translations require explicit human review confirmation.");status="Published";Object.assign(update,{status,reviewed_by:user.id,reviewed_at:new Date().toISOString(),published_by:user.id,published_at:new Date().toISOString(),machine_generated:false})}let data;if(id){const result=await admin.from("translation_entries").update(update).eq("id",id).eq("version",expectedVersion).select().single();if(result.error)throw result.error;data=result.data}else{const result=await admin.from("translation_entries").insert({...existing,...update}).select().single();if(result.error)throw result.error;data=result.data}await admin.from("admin_security_events").insert({actor_user_id:user.id,action:`translation_${status.toLowerCase()}`,details:{translation_key:existing.translation_key,locale:existing.locale,before:existing.translated_text,after:text,impact_level:existing.impact_level}});return Response.json({entry:data})}catch(error){console.error("Translation manager save failed",error);return Response.json({error:error instanceof Error?error.message:"Unable to save translation."},{status:400})}}
