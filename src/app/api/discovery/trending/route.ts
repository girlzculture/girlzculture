import { cleanText,errorResponse } from "@/lib/requestSecurity";
import { normalizeRadius,validCoordinates } from "@/lib/location";
import { supabase } from "@/lib/supabase";

export async function GET(request:Request){try{const search=new URL(request.url).searchParams;const origin={lat:Number(search.get("lat")),lng:Number(search.get("lng"))};if(!validCoordinates(origin))throw new Error("Choose a valid location to see Trending Picks.");const limit=Math.max(1,Math.min(50,Math.round(Number(search.get("limit")||12))));const offset=Math.max(0,Math.round(Number(search.get("offset")||0)));const seed=cleanText(search.get("seed"),100)||new Date().toISOString().slice(0,13);const{data,error}=await supabase.rpc("discover_trending_videos",{origin_latitude:origin.lat,origin_longitude:origin.lng,request_radius_miles:normalizeRadius(search.get("radius")),rotation_seed:seed,result_limit:limit,result_offset:offset});if(error)throw error;const videos=Array.isArray(data)?data:[];return Response.json({videos,total:Number(videos[0]?.total_count||0)},{headers:{"Cache-Control":"public, max-age=60, stale-while-revalidate=300"}})}catch(error){console.error("Trending discovery failed",error);return errorResponse(error,"Trending Picks could not be loaded.")}}
