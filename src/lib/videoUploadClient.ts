export const MAX_TRENDING_VIDEO_BYTES=25*1024*1024;
const TARGET_TRENDING_VIDEO_BYTES=10*1024*1024;
export async function getVideoDuration(file:File){const url=URL.createObjectURL(file);try{return await new Promise<number>((resolve,reject)=>{const video=document.createElement("video");video.preload="metadata";video.onloadedmetadata=()=>resolve(video.duration);video.onerror=()=>reject(new Error("Unable to read this video."));video.src=url})}finally{URL.revokeObjectURL(url)}}
export async function optimizeTrendingVideo(file:File){if(!["video/mp4","video/webm"].includes(file.type))throw new Error("Upload an MP4 or WebM video.");const duration=await getVideoDuration(file);if(!(duration>0&&duration<=30.5))throw new Error("Trending videos must be 30 seconds or shorter.");if(file.size<=TARGET_TRENDING_VIDEO_BYTES)return{file,duration};const video=document.createElement("video")as HTMLVideoElement&{captureStream?:()=>MediaStream};if(typeof MediaRecorder==="undefined"||!video.captureStream)throw new Error("This browser cannot optimize the clip. Export it under 10 MB and upload it again.");const url=URL.createObjectURL(file);try{video.src=url;video.muted=true;video.playsInline=true;await new Promise<void>((resolve,reject)=>{video.onloadeddata=()=>resolve();video.onerror=()=>reject(new Error("Unable to prepare this video."))});const stream=video.captureStream();const mime=MediaRecorder.isTypeSupported("video/webm;codecs=vp8,opus")?"video/webm;codecs=vp8,opus":"video/webm";const recorder=new MediaRecorder(stream,{mimeType:mime,videoBitsPerSecond:1_800_000,audioBitsPerSecond:96_000});const chunks:Blob[]=[];recorder.ondataavailable=(event)=>{if(event.data.size)chunks.push(event.data)};const finished=new Promise<void>((resolve,reject)=>{recorder.onstop=()=>resolve();recorder.onerror=()=>reject(new Error("Video optimization failed."))});recorder.start(500);await video.play();await new Promise<void>((resolve)=>{video.onended=()=>resolve();window.setTimeout(resolve,Math.ceil(duration*1000)+1500)});if(recorder.state!=="inactive")recorder.stop();await finished;const blob=new Blob(chunks,{type:"video/webm"});const optimized=blob.size&&blob.size<file.size?new File([blob],`${file.name.replace(/\.[^.]+$/,"")}.webm`,{type:"video/webm",lastModified:Date.now()}):file;if(optimized.size>MAX_TRENDING_VIDEO_BYTES)throw new Error("The optimized video is still over 25 MB. Export a shorter or lower-resolution clip.");return{file:optimized,duration:await getVideoDuration(optimized)}}finally{URL.revokeObjectURL(url)}}
