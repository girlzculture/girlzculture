# Dashboard and business-software implementation checkpoint

This branch is in progress. It is not merged, deployed, or a completed 140-item release.

Base: `d37cbf4ef3954063797946b7508352470653a739`.
Branch: `codex/business-dashboard-value-redesign`.
Production last checked at 2026-09-18 17:13 UTC: locked deployment `6aac503fa5eacb00089c6d87`, same base commit. Rollback: `6aac3c4f2da488000962085f` / `525f4103a778b95290885ee15e7bf1ffee8839af`.

## Binding decisions

- Percentage promotions use eligible service price: $100 price, $10 protected deposit, 20% offer gives $20 saving and $70 remaining balance. Cap saving at the unpaid balance; preserve existing booking snapshots.
- No-show protection uses only incidents at the current business. Cross-business blacklists, scores and even anonymized eligibility indicators are forbidden. This supersedes the attachment's across-platform wording.
- Assistant data access is current authorized business only, even for another business's public records. Ordinary customer public browsing remains available.
- Existing OpenAI and DeepL integrations, both $25 caps, supported EN/FR/ES/zh-CN behavior, opt-in memory and calendar safeguards must remain. Wolof remains deferred.
- Full implementation, protected reviewed migrations, merge and publication are authorized once release gates pass. No additional general deployment approval is needed. PRs #59/#60 and unrelated parked work remain untouched.

## Source and tracking

`founder-handoff.md` is an unchanged copy of the complete attached brief. `requirements.json` contains all 140 unique IDs with full indexed definitions, detailed-section references, initial code mappings, changes and evidence fields. All detailed clauses apply; the short index does not reduce scope. Two initial mapping paths need correction as discovered: BookingNotes is under `src/components/`, not `src/components/owner/`; new modules must be added to the map when implemented.

All 19 supplied inline images were inspected: ten dashboard target composites, three public inspiration composites, two mobile assistant defect captures and int10–int13 defect captures. Preserve the root business landing. `/site-access` must become the real customer entry; public layout is selective polish, not a mockup replacement. The decorative poster/phone frames and invented figures in references are not app content.

## Work implemented locally

1. Chat submission now inserts the user's message and clears the composer before awaiting the server. An in-flight guard prevents duplicate submits. Failure retains the turn and exact reference; explicit retry keeps its ID/context and preserves the next draft. Actor changes discard stale results. New replies do not pull readers away from earlier messages. IME and Shift+Enter remain supported.
2. Persistent desktop assistant uses a nonmodal adjacent panel at desktop widths; the workspace reserves its width. Phones/tablets open on tap. Close/reopen and desktop route continuity work. Shared header combines the page search and assistant launcher, removing the redundant strips. These are foundational changes, not all fourteen page redesigns.
3. Finances label replaces Earnings & Payouts in owner navigation, title, assistant destination and permission labels; existing route/permission identifiers remain stable. New copy has FR/ES/zh-CN baseline translations. Per-reply audio explanations are collapsed into one panel disclosure; explicit read-aloud controls remain.
4. Policy publishing hashes canonical key order. Genuine stale previews retain code HTTP409 and the exact protected reference instead of becoming a generic HTTP500 through monitoring.
5. Policy editor now uses primary Business Policy text, optional preparation, a source-language selector and explicit booking-rule controls. Legacy terms convert without altering published history; enforced rules stay structured. Initial-load failure disables saving defaults over existing policy and offers reload. New prose must pass validation. A pending migration expands JSON size capacity for multibyte prose plus retained legacy fields; not applied yet.

## Demonstrated defects and evidence

- Baseline chat browser test failed before the response arrived: submitted text absent from all articles. Original screenshot/trace retained outside Git in `../redesign-evidence/chat-before/`. The dependency junction first caused a Turbopack setup failure; replaced with `npm ci --offline` from the unchanged lockfile. No dependency changes.
- After correction, 18/18 new Chromium/WebKit tests passed in `tests/browser/p0-assistant-interaction.spec.ts`, covering 390x844, 768x900, 1440x900, 844x390 and IME/history behavior. WebKit emitted a dev-HMR chunk-load message; final production-build browser verification remains required.
- Five actor/session tests passed after optimistic submission; a failed submission now correctly retains one turn instead of zero, with raw-body exclusion and a retry assertion.
- Policy incident `70ad1644-c08f-4a16-af76-014d9b58b0df` was inspected read-only. It records six `/api/salon/policies` POST conflicts, HTTP409, between 2026-09-17 22:20 and 23:06 UTC. No private business data or credentials were read. The code separately reproduced unchanged input becoming `POLICY_PREVIEW_STALE` after a JSONB-style key reorder. Canonical hashing passes that case and still rejects an actual prose change. Monitoring's generic wrapper explained why the UI displayed unavailable rather than a recoverable conflict. The coarse historical event does not independently prove every one of the six requests had identical input.
- Policy tests: 3 editor/conversion, 3 API/audit, 4 disclosure tests passing. Browser editor/save/review/publish/refresh/failure recovery and public acknowledgment coverage still needs updating for the expressly requested new editor.
- TypeScript passed after chat/layout changes; a later TypeScript check after policy changes was still running at this checkpoint.

## Next work, without restarting completed checks

1. Finish policy browser regression and public presentation; add its new primary text to assistant draft presentation. Connect Walk-ins welcome in My Page/public profile without changing booking capacity. Verify migration locally before protected application.
2. Implement service search matching and truthful inventory counts. Current query filters `styles.name` before counting, so no match is incorrectly represented as total zero; no photo read tool exists. Add scoped media inventory and server-side permissions, update planner/presentation/registry migrations, prove two-business isolation before live provider checks.
3. Finish all fourteen page compositions and details, saved business assistant avatars, media categories/editor, owner finance/off-platform ledger, deposits/compensation/expenses/stock/reports/permissions, client history, communications, own-business no-show rules and appointment waitlist. All remain required; none is deferred.
4. Resolve subscription method-management defect through existing Stripe integration without charges. Implement approved promotion math consistently with server booking snapshots and public evidence.
5. `/site-access` currently delegates to the customer Home component but retains demonstration metadata and gate/banner wrappers. Preserve approved root routing, remove public demo restrictions only for real eligible businesses, restore customer navigation/Pricing placement and selective public polish.
6. Complete updated ledger clauses, matched visual evidence, required checks on final source, reviewed pending migrations, held candidate provider checks, merge and verified production publication. No new PR or workflow has been created/dispatched yet.

Prior main release CI `35272232878` passed 580 tests with five existing viewport skips; DeepL and memory migrations already applied. Reuse unchanged provider/language/calendar evidence, rerun only where these changes invalidate it. No production writes, billing changes, cap changes or deployments have been made for this redesign so far.

## Continuation — 18 September, local foundation verified

- Service search now ranks current-business alias/typo candidates while distinguishing inventory size, matching records, unavailable counts and bounded/incomplete searches. Actual service facts remain unchanged. Planning tests31/31; execution tests14/14 at media-read introduction. Full module coverage remains required.
- Added authenticated get_business_media read (photos permission), distinct gallery/cover/logo counts, category/caption metadata and honest unknown publication status. General platform knowledge is limited to six general CMS slugs; no public-business lookup.
- My Page now presents actual editable cover/logo, identity, completion and mobile Info/Services/Location/More. Walk-ins is persisted in existing trust_info and exposed on the correct public profile. Mixed profile patches require every affected permission; asynchronous updates check business identity. A selected walk-in option clears the incompatible appointment-only option, and the backend independently validates both.
- Photos opens the real gallery with categories, edit title/caption/featured controls and existing crop/remove/order pipeline as a secondary disclosure. Category assignment uses the selected upload category, retains failures for retry. Atomic photo metadata RPC validates authorization, existing gallery membership, prior revision and merges per-image edits. Source prose remains unchanged. Real upload/category persistence and live SQL gates are still pending.
- Policy prose included in assistant moderation and public disclosure. Chromium390 policy edit/save/review/publish/refresh/failure test passed before the later My Page tab change; final combined policy/booking acknowledgment coverage remains necessary.
- Assistant business preference now offers six avatar choices, displayed in launcher/header/messages. Only owners can update, with exact error reference and verified persisted response. Roles and business context reset stale conversation, request IDs and optional memory references. Current-page quick actions (3–5 before staff filtering) cover all14 owner routes. Source copy includes FR/ES/zh-CN baseline translations; no native-quality/provider claim.
- Latest combined browser run:40/40 passed in3.1minutes, Chromium and WebKit, dashboard profile/photo flows (fourviewports), avatar retry/persistence/context reset (phone/desktop), all prior chat/IME/retry/scroll and opt-in-memory persistence/exact-reference cases. Browser fixtures are explicitly local; the context-switch case simulates a newly authorized server workspace. Two-business API tests reject foreign IDs/URLs and staff appearance writes. Full live isolation remains pending.
- TypeScript and targeted ESLint passed after these changes. Dev-only WebKit HMR chunk warnings persist; production-build browser verification is still required. No arbitrary sleeps, retries or skipped failing assertions were introduced.
- Four pending, unapplied migrations:20260918024000policycapacity,20260918031000mediareadregistry,20260918033000photodetails,20260918040000appearance. Added disposable rollback-only database assertions to existing clean-DB verification. PostgreSQL tools are not currently onPATH; SQL not yet executed. Latest migration marker advances only integrations.expected_migration; bothAIcaps untouched.
- Read-only billing incident:b19ef999-8b7b-4a21-811f-6e1de5a443cf, two portal failures Sep17 22:17:21 and23:34:32UTC, StripeHTTP400. Existing helper drops provider reason, so rootcause not established. No payment/card/subscription changes made.
- Browser inventory now has no earlier authenticated tabs. Do not ask to sign in during independent implementation. Reuse a stable final candidate and request the minimum genuinelyneeded role only at live verification. New background public baseline tab27 shows root stillbusinessonboarding and /site-access stilldemo banner with booking/payment disabled; this is the reproduced public defect.
- Remaining next:public marketplace/demo/navigation corrections, allremaining page compositions and finance/client/communications/stock/waitlist/deposit capabilities, safe billing diagnostics/correction, source-translation inventory, cleanDB/build/requiredCI, reviewed migrations, heldcandidate and publishverifiedfinalsource. These are all required. No newPR/push/workflow/productionmutation/deploy yet.


## Continuation — public marketplace and operating-book foundations

- Committed foundation remains2c2cd69. Subsequent public and finance changes are currently uncommitted; no redesign branch push/PR/CI/deploy or production migration has occurred.
- Root business landing remains intact; /site-access now runs the real customer Home. Removed public demo/banner/provider and prelaunch API gate, retained authorization/eligible-business/payment checks. Customer Home points/site-access. Explore and For Businesses groups retain published CMS routes, business center/login and /plans. Coming-soon business-category waitlist CTAs are filtered from customer home without affecting appointment waitlists. Service worker cache revision advanced to retire old presentation.
- Browser public checks:22/24 first legacy-gate run passed; two failures were a newly written assertion aimed at nonexistent /api/customer/account. Corrected to actual protected /api/customer/favorites GET/POST/DELETE; focused Chromium/WebKit2/2 passed. Browse Styles restoration, mobile hydration and grouped responsive navigation3/3 passed. Existing public-policy suite first failed at501 fixture setup; fresh fixture endpoint returned200. Config now explicitly passes fixture URL and forbids reusing old fixture/server processes;6/6 policy tests passed on fresh3106/3107. No assertion weakened. Screenshot marketplace390 captured skeletons: replace with loaded-state evidence before acceptance.
- Reproduced old financeLedgerCore /paid/ substring counting Unpaid as paid; replaced with exact normalized accepted statuses. New integer-cent operating-books core separates sale, receipt method, source and stage; refunds retain original links; compensation deductions/payouts and inventory purchases/COGS are not double counted; profit qualified for incomplete costs.
- New pending migration20260918050000 adds external sales/receipts/expenses, immutable arrangement versions, wage/rent obligations and payout records, audited request-id deduplication, protected read/log/manage RPCs. Provider booking records are read in place, not copied into duplicate sales. Current own-stylist RPC projects only that business/stylist before model/UI; front desk log permission does not grant finance totals. Old bookings retain none/unknown arrangement snapshots instead of invented historic commission.
- Operating finance UI and assistant read path connected. Scope/body/foreign-ID tests added; assistant returns aggregates instead of raw books/client/receipt IDs. Explicitly incomplete: online product-order reconciliation, full compensation UI/automation/outstanding, exports, date controls, all finance localization and remaining detailed clauses. Do not label these finance requirements complete.
- Local portable PostgreSQL17.11 (official EDB ZIP, no installation/service) runs127.0.0.1:55432 in ../local-postgres-17/fixture-data. PSQL_BIN points ../local-postgres-17/pgsql/bin/psql.exe. Databases girlzculture_clean(initial155chain),girlzculture_finance_clean(full156chain) are disposable test data, no production credentials/records. New dashboard fixture used illegal identity status Suspended; corrected to actual Disabled without changing schema. Both new SQL suites now pass. Full156chain plus existing assertions passed on empty girlzculture_finance_clean. Log ../redesign-evidence/finance-database-clean.log. Stop local server with pg_ctl against exactfixture-data when no longer needed.
- Finance core10/10, projection/isolation6/6, route/auth/idempotency5/5 passed. Existing assistant planning31/31, operational18/18 after fixtures represent protected financeRPC. TypeScript/latest targeted lint passed. New9-case Chromium/WebKit finance browser suite is running; follow existing process, do not restart unchanged.
- Both USD25 caps unchanged; no real provider requests, Stripe changes or production writes. Remaining scope still all140 detailed requirements, with Wolof alone explicitly deferred.

## Continuation — scoped finance history, reports and production-build check

- All 140 ledger entries now include the complete detailed clause text and source line, in addition to the checklist label. Implemented subsets remain AUTOMATED ONLY until live acceptance; the release is incomplete.
- Completed online product payment/refund projection, integer-cent merchandise/tax/shipping separation, business-time-zone date presets, immutable compensation arrangements, wage/rent obligations and partial payments, and as-of outstanding/advance positions. Own-stylist projections exclude other staff, business expenses and product totals before UI/model/export access.
- Reproduced stale assistant history disclosure after finance permission reduction or stylist reassignment. Backend planning and execution now restrict old history, re-read current authorized results on replay, and omit transcript derived from rejected results. Pending migration 20260918060000 applies equivalent saved-history authorization. Direct, follow-up and forged-ID tests use two separate business fixtures. Full clean local database chain:157 migrations plus assertions passed; no production change.
- Latest focused coverage before report integration: finance core12/12, projection10/10, route5/5, planning33/33 and execution15/15. Finance browser12/12 covered Chromium/WebKit, four viewports, failed-save retention/idempotent explicit retry, logging-only staff and wage obligation/partial-payment refresh.
- Added current-permission PDF/XLSX exports for EN/FR/ES/zh-CN with shared ledger totals, periods, currency, refund links and bounded rows. PDFKit0.20.2 and its type package are the only dependency additions; existing ExcelJS retained. Bundled licensed Noto font supports Chinese; production route trace includes it. Export/auth/readback tests5/5 passed; four three-page PDFs checked by text extraction and all twelve pages visually inspected. Spreadsheet strings that resemble formulas remain strings. These checks use synthetic local data only.
- Windows Playwright WebKit reproduced hairline Montserrat with the former Google-served font. Four pinned official static Montserrat weights are now served locally; same-browser comparison and Chromium/WebKit390 finance checks2/2 passed. Engine font-family choices remain intact.
- Preserved development-server404 traces and corrupted generated route/type files under ../redesign-evidence. Clean production build passed, then all8 export download/failure-reference tests passed in Chromium/WebKit across four locales. The 404 did not reproduce in production mode; its underlying development artifact corruption is not claimed proven. No assertion weakened or automatic retry added.
- Confirmed the next payment defect in code: checkout computes the deposit after a salon discount and also permits Stripe coupons against the deposit. Correction must protect the pre-discount applicable business deposit, cap discounts at the unpaid balance, retain old snapshots, and align public previews/booking/finance evidence. Not corrected at this checkpoint.
- Current required work still includes owner deposit/no-show controls, promotion math, billing-method correction, remaining dashboard compositions, client histories/formulas, communications, waitlists and stock, full clause verification, final CI/protected migrations, stable candidate/live acceptance and authorized publication. No redesign push/PR/deploy or production mutation yet; both25-dollar caps unchanged.

## Continuation — protected offers, own-business attendance and calendar-first work

- Latest committed source 6bc4f07. Subsequent changes remain local/uncommitted; no redesign push, PR, workflow or production mutation.
- Generated pending migration 20260918072308_business_deposit_rules_and_protected_offers.sql with pinned CLI. All158 migrations and new deposit assertions pass on fresh local girlzculture_deposit_clean. Final SQL includes service-role read-only table privileges, guarded RPC writes, immutable booking snapshots, owner CAS/idempotent rule publication, own-business incidents, assigned-staff restrictions, customer and verified booking-token review requests, correction/void history. No production application.
- Approved promotion arithmetic enforced through search/public prices and checkout. Discounts cannot consume the protected deposit; no stacking or product spillover. Changed price/deposit returns409 before reservation/Stripe and requires fresh financial consent while retaining guest input. Fixed no-payment actual bookings being labeled test records and missing deposit snapshot origin. No real charge was run.
- Deposit-family Node21/21 and browser14/14 cases passed before later UI work:10 translated deposit/attendance cases and4 price-change cases, both Chromium/WebKit. Browser failures were exact-name/role selectors (currency decimals; financial checkbox; controlled labels), corrected without weaker behavior assertions. Evidence outside Git in redesign-evidence. Current combined rerun validates subsequent public saving-copy and nav changes.
- Foreign/missing assistant history ID regression reproduced client prose reaching the model; fixed pre-provider by checking all requested IDs against freshly authorized rows. Planner34/34 passes, including clarifications and EN/FR/ES/zh-CN continuity.
- Overview now composes real timezone/date, period metrics, six-month trend, today/upcoming, page health and quick actions. Calculation5/5 passes. Calendar first, staff filters, phone agenda and spanning closures implemented. Legacy five workspace routes remain. Old-build missing calendar reproduced. Production build and TypeScript pass; browser run currently contains new test label-selector failures, not yet a final visual acceptance.
- Used Supabase Postgres best-practice guidance for upcoming client-data/RLS schema work; no new client migration yet. New module remains required.
- Still required: full remaining dashboard/page/dialog compositions; client formula/caution/photo/history permissions; grounded advisor tools; chat/reminders/campaign requirements; appointment waitlist/atomic claim; supplies/stock movements; billing-method root cause and real secure flow; remaining finance reports/detail clauses, translation inventory, all140 evidence and final reviewed release. Only Wolof deferred. Both USD25 caps unchanged.

- Final compact Overview/Calendar production-build verification:8/8 passed across Chromium/WebKit390,768,1440 and landscape844. Desktop and phone viewport captures visually inspected; setup below workspace, compact alert/status controls, no overflow. New selector failure was fixed by targeting the actual combobox role; all original behavioral assertions retained. Latest focused ESLint zero warnings/errors; production build/TypeScript pass. npm audit --audit-level=high passes with zero vulnerabilities.
- Added regression reproducing missing authoritative deposit rules in assistant policy answers. Own-business rule now returned with pre-discount basis, highest-rate-once, protected-deposit and unchanged-old-booking facts; no incident/person records fetched. Assistant execution16/16 passed. Full required CI and live acceptance remain pending.


## Continuation — private client records and assignment isolation

- New local module: pending migration20260918091453; business-specific client cards/preferences/notes/cautions, exact original-language per-visit formulas, private work photos, linked visit/spend history. Seven owner-controlled field/edit permissions; relevant-stylist assignment enforced. Atomic revisions, request-id retry, immutable audit and original field locales. Registered returning customer shares only that business's record; typed guest email/name never implicitly links identity. Guest manual linking and off-platform sale linking remain to finish.
- All159 migrations executed on fresh local girlzculture_clients_clean. Rollback-only SQL uses two businesses sharing a customer and proves foreign-business/booking denial, assigned-stylist/field isolation, role revocation, CAS/retry/audit, original formulas, private photo authorization and Storage RLS under actual browser roles. No production migration.
- New card UI in booking details: EN/FR/ES/zh-CN, retained failed-save draft, explicit retry same ID, readback/reload, private blob image upload/read/removal. First5 browser failures were label selectors before action execution; preserved traces and targeted actual roles without changing behavior. Final10/10 Chromium+WebKit passed across390/768/1440/844landscape. A server destination-stream-close log appeared during reload; no failing browser assertion. Captures show a section screenshot crossing sticky chrome, not an overflow; use viewport captures for final visual evidence. Added missing Size/Length translations.
- Invalid image reproduced500; corrected to secret-safe400 before upload. Client-error codes use an explicit allowlist, never database/body/private-prose logs. New client route6 tests; malformed image and forged IDs covered. Planner contract/memory disallow private cards in saved memory.
- Reproduced older assistant assignment gap: get_bookings and related reads included another staff member's appointments at the same business. Query filters now apply current assigned professional to bookings/customers/upcoming/summary/messages/calendar and customer-message preparation. Reassigned history is refreshed through authorized reads before model; invalidated transcript discarded. Saved proposal replay and confirmation also check assignment before returning private fields. Foreign business requests were already denied and remain denied.
- Combined118 Node tests passed before final proposal-replay guard; subsequent execution+operational36/36 passed including that regression. TypeScript, changed-file ESLint and latest production build passed. Final strict error-code allowlist added afterward with focused validation. Build/browser evidence is local fixtures, not real-provider/live acceptance.
- Latest committed foundation3574741; current client work not yet committed. No redesign push/PR, remoteCI, production migration or deployment. Both USD25 caps unchanged. Continue complete140-clause implementation; only Wolof deferred. Remaining page designs/features, communications, waitlist, inventory, billing and live protected release still required.

## Continuation — catalog and team workspace

- Prior client changes committed as911b84a. Subsequent catalog/team work is local and pending commit; no remote PR/CI, production migration or Netlify mutation. Production remains the previous release; bothUSD25 caps unchanged.
- Service cards, actual category/price/duration/media, editable featured flag distinct from completed-booking popularity. Popular=top3 eligible services by distinct completed non-test own-business appointments in past90days. Search/category/group/status/sort/view in URL survive edit/back/reload. Import/export remains accessible below main catalog. Public business menu stable featured-first; original catalog order within groups. New pending migration20260918101950 generated byCLI.
- Reproduced workspace's1000-row silent truncation; paginate all authorized non-review tables with fail-closed100000bound. Assignment-filter bookings/blockouts/notices before response. Regression two businesses and assigned staff. Reviews' older single-page path remains to examine with Reviews work.
- New controlled feature toggle waits for verified server result; failed save preserves prior state and exact safe support reference. Browser found accessibility aria-label translated service names; replaced with aria-labelledby pointing at original-language heading. Found and corrected missing setStyles readback. Existing test targeted immediate checkbox check; click+explicit saved-state assertion now reflects verified asynchronous behavior without weakening outcome assertions.
- Full clean DB caught insert field/value mismatch in new atomic style RPC; fixed is_featured insertion column and added direct creation regression. All160 migrations and assertions pass on fresh local girlzculture_catalog_verified. Earlier failed evidence preservedcatalog-clean-chain.log. Catalog core/workspace6passed; SQL/P0 assistant draft creation78assertionspass. No production schema changes.
- Catalog browser8/8 across Chromium/WebKit EN390 FR768 ES1440 zhCN844landscape. Visual review fixed undefined border/background theme classes and missing Featured/Braiding translations; later2catalog ES cases pass and screenshots inspected. Full service form remains, with larger controls. Existing catalog sort-order/import refresh and late-default service-draft tests8/8pass in twoengines. Public live ordering acceptance still required.
- Team: equal responsive cards, full original specialty chips, URL-selected person and filters, monthly own-business/permitted appointment performance, amount!=cash/pay, actual saved profile readiness, schedules, selected-person deep link, existing compensation and owner-only team settings. New core2passed; original8browserpassed. Phone review found first card belowfold; compact mobile view selector/More menu and2columnfilters, selected detail moves above roster/focuses on explicit selection. Additional4phone/landscape casespass includingfirstcardviewport. Final More label shortened after visualreview; includein nextbuild. Assignment capability still required, no decorative completed claim.
- Latest production build TypeScriptpass, changed-fileESLintpass before last compact changes. team-catalog-browser.log16pass includes destination-stream-close during reload; no assertionfailure, not claimed proven harmless/rootcaused. Teamcompact4pass. Team/catalogcore8pass. Source changes notyetremoteCI/live.
- Next: implement real own-business stylist-service assignments with persistence and booking enforcement; finish remaining page redesigns and features, private guest/offline client linking, communications, atomic appointmentwaitlist, supplies/stock/finance dailyclose, billing rootcause, all140 clause evidence, requiredCI/reviewed migrations/stablecandidate/authorized publication and publicchecks. OnlyWolofdeferred. No new scope question needed.

## Professional service assignments (after ae121ec)

- Implemented explicit all/current-and-future (NULL), subset, and none ({}) assignments in Team details. Existing profiles retain all-service behavior. Failed saves retain the draft and the exact incident reference; verified save updates the roster. Readback/reload covered in four languages.
- Public booking filters the roster for the selected service and clears an ineligible selected professional before requesting availability. Server availability excludes inactive/draft/unassigned professionals without a salon-resource fallback. General calendar hours remain independent.
- New migration 20260918112223 validates own-business service IDs and permission at the database boundary, serializes changes with the calendar lock, and guards reservation creation. Holds created before a later assignment edit can still complete; existing booked amounts/terms are unchanged. Checkout returns a scoped 409 if an assignment changed after preflight.
- Assistant professionals read includes an own-business bounded service dictionary only for roles with catalog access. Direct and follow-up requests for either of two businesses, forged business query, mixed foreign assignment IDs, and catalog revocation covered before model input. No real provider requests.
- Reproduced before fix: unassigned professional offered18 slots (assignments-before.log). New SQL test caught unqualified service-ID correlation in the new guard; qualified it and clean161 replay passes. New browser failure reproduced a null save-result dereference; opt-in throwOnFailure preserves shared callers and lets the new editor show the reference/draft instead of crashing. Original failures kept under assignments-first-browser.
- Final evidence ../redesign-evidence: assignments-browser-final.log12/12 (8 owner EN/FR/ES/zh-CN +4 public390/1440, Chromium/WebKit); assignments-availability.log4; assignments-core-routes-fixed.log5; assignments-assistant.log18; assignments-clean-chain.log161 migrations/all SQL assertions; assignments-build-fixed.log and assignments-types-final.log pass. Final changed-file lint pending at checkpoint creation. Eight existing team browser cases passed before save-helper correction (default helper behavior unchanged).
- Local latest fresh database: girlzculture_assignments_clean, localhost55432. Production untouched, no push/PR, no remoteCI. Both caps unchanged. Destination-stream-close log on reload preserved; no hydration/assertion failure, cause not claimed established.
- Catalog/team prior commit is ae121ec, superseding prior checkpoint pending-commit wording. Continue Products/inventory, remaining page designs and detailed finance/client/advisor/comms/waitlist/billing/public requirements. Do not claim all140 or release complete.
- Final assignment changed-file lint: zero errors and zero warnings; git diff --check passed.

## Products catalog continuation

- Assignment work committed c8f9eb8. Products catalog now has actual stock states (unknown never zero), own active-offer markers, image/cards/list, URL filters retained through edit/back/reload, import/export secondary. Original missing workspace reproduced and preserved in products-before. Mobile screenshot prompted compact filters and horizontal cards, with first product name required in initial viewport. Desktop and phone captures inspected.
- Evidence ../redesign-evidence/products-browser-final.log16/16 Chromium+WebKit four languages/viewports plus original imported order checks; products-core.log2; products-build-compact.log and products-types.log pass; products-lint-final.log0errors0warnings. Reload destination-stream-close log remains recorded, no assertion failures.
- Not released. Inventory supplies/restock/correction ledger, stock-linked finance and remaining full140 scope still required. Next correction: stale full-product forms currently write absolute stock; separate guarded adjustments and immutable history without duplicating checkout deductions. Both caps unchanged; no production mutation.

## Private inventory and stock-linked finance

- Prior Products catalog committed1491c05. Pending migration20260918122816 adds private supplies, immutable quantity movements and guarded read/write RPCs. Same-business permissions, current identity locks, plan checks, revision conflicts and request idempotency. Trigger observes existing checkout stock changes once, with no extra deduction. New offline catalog product sale uses canonical own identity/quantity and deducts once atomically. Receipt/balance/refund paths do not deduct/restock a second time. Restock expense optional: retail inventory_asset, supplies operating; sold cost recorded explicitly so profit avoids double deduction.
- Reproduced stale catalog form overwrite (stock-stale-before.log). Existing product metadata form omits quantity; sanitizer rejects absolute updates. Spreadsheet updates preserve current quantities and explain the dedicated adjustment workflow. Actual reserve_combined_checkout/release_combined_checkout and retries, stale import, foreign product/business, revoked staff, insufficient stock, supply usage, expense treatment and direct browser denial all pass rollback-only SQL.
- Initial migration assembly had dollar-quote escaping errors; corrected before any commit/application beyond disposable local DB. First clean-chain fixture cleanup failed because audit rows prevented business deletion; canonical business deletion now cascades new private stock rows, while routine product removal remains archival. Full162 chain and all assertions pass on girlzculture_inventory_verified; stock-clean-final.log. Final additional real checkout/import assertions pass stock-database-final.log.
- Browser first run16new failures/16existing passes; originals under stock-first-browser. Invalid fake incident references were replaced by valid UUID fixtures; expected UUID assertion retained. New finance test now targets visible desktop row/mobile card and distinct saved status. Actual product name Save was translated to Enregistrer; finance business names and product/stylist option labels now data-no-translate. No assertions weakened/retries/sleeps added.
- Final stock-browser-final.log24/24 (16 new inventory/product-sale cases and8 existingfinance cases); four locales/viewports and both engines. Earlier8 existingProducts cases also passed after nav/edit changes. PhoneWebKit and desktopChromium screenshots inspected. Stock route3/3; stock-finance-core.log33/33; operational server20/20 in stock-core-final.log (that combined log has one test-file syntax failure fixed separately by stock-route.log). Latest stock-build-verified.log, stock-types-final.log and stock-lint-final.log pass;0lint warnings/errors.
- No production changes/push/PR. Caps unchanged. Need remaining page/detail designs, guest/offline client linking, grounded advisor gaps, communications, waitlist, billing rootcause, detailed requirement evidence, requiredCI/protected migration/heldcandidate and authorized publication. Only Wolof deferred.

## 18 September — explicit private guest history links
- Prior stock/finance work committed7fc6e6f. New pending migration20260918131641 (163rd) associates guest visits only after owner review, within the authenticated business. Original card fields/formulas/photos stay intact; no customer Auth identity changes and no name/email auto-match. Owner can unlink. Registered identities cannot join even through a guest bridge. Owner-only search/action, fresh scope/field/assigned-stylist reads, bounded graph, CAS/idempotence/audit and private no-store routes.
- Linked history/spend excludes other businesses. Original notes/cautions remain separately labeled; linked private photo access reauthorizes each read. Unlink/permission changes reproject old assistant results and discard stale conversational facts before model calls.
- Evidence ../redesign-evidence/client-links-core.log46/46; client-links-db.log own/two-business/roles/idempotence/conflict/identity bridge/unlink passed. client-links-clean.log all163 migrations and SQL assertions passed on new local girlzculture_client_links_clean. client-links-build.log, client-links-types.log and lint0/0 passed.
- Browser client-links-browser.log14/14 (Chromium new4+existing clientcards10). Initial config did not select new WebKit cases; added them to testMatch/grep then client-links-webkit.log4/4. Total18, no skips/retries/sleeps. Saved values and failure drafts, exact incident reference, review before writes, same-ID retry, refresh, unlink and original formula/caution preservation inEN/FR/ES/zhCN; 390/768/1440/844 landscape. DesktopChromium and phoneWebKit screenshots inspected. SQL fixture setup initially used invalid owner/customer identity and omitted required deposit; corrected fixture, not application guard.
- No remote publication or production data action. Both caps unchanged. Still required: offline finance client association, remaining page/detail redesigns, advisor/comms/waitlist/billing requirements, final140-clause/live proof, required CI/protected migrations/stablecandidate/authorized release. OnlyWolofdeferred.

## Bookings operational workspace

- Prior guest-history work committed36d20d6. Bookings now has truthful date/staff/service summaries, local-day filters, source/deposit/balance/status detail, compact phone cards and 25-record pagination. Existing detail/manual/cancel/conflict paths preserved. Native History merges latest URL synchronously; reproduced router-replace filter race no longer loses staff after a rapid service change.
- WebKit trace additionally reproduced native smooth scrolling moving Apply between pointer events; sticky chrome intercepted activation. Extended existing manual-editor instant-scroll rule to the owner workspace and set scroll padding. Original assertions pass. Phone visual review tightened first-record visibility to require its bottom above the fixed nav, rather than only intersecting the viewport.
- Evidence:bookings-final-browser.log10/10 Chromium/WebKit EN/FR/ES/zhCN, phone pagination/reload; original calendar/service-name15passed; client-link narrow controls8passed; bookings-core-final.log4; buildbookings-final-build.log and focused lint/types passed. Desktop/phone captures inspected. Failed traces preserved in bookings-compact-failure and bookings-filter-race.
- Reviews next: reproduced moderation-held reply falsely rendered public; reviews-pagination-before.log proves silent1000-row cap (expected1002). Full remaining140scope continues. No redesign PR/push/prodchange; both caps unchanged.

## Reviews workspace and guarded reply revisions

- Bookings committedae64a2a. Reproduced Reviews holding a reply for moderation yet rendering it as a public reply (reviews-pending-before), and silently returning1000of1002reviews (reviews-pagination-before.log). Corrected authoritative reply readback and paged own-business review/events/queue projections. No private moderation/provider internals returned.
- Redesigned list/selected detail/mobile focused view, actual public-only metrics and six activity-month trend (no synthetic sentiment), original-language review/reply text, rating/service/search/status URL filters,20-row pages, moderation/dispute/original-reply history. Existing customer text remains immutable.
- Pending migration20260918142356 adds private immutable request/reply versions and current revision guard. Fresh identity/business/Reviews permission authorization before mutation, atomic parent/queue lock order, request dedupe, pending edits retain public text, clear edits invalidate prior pending text, moderator/legacy publication advances revision. Request replay returns current public/queue state. Stale editor can load current reply while retaining draft, then explicitly save.
- Evidence:reviews-core-final.log10passed; reviews-browser.log10passed; reviews-concurrency-pagination-final.log4passed in Chromium/WebKit; four required locales and viewports. Additional-test failures were unscoped Next route-announcer alert and Back called before actual navigation; scoped error assertion and explicit detail URL assertion fixed tests without changing behavior. Preserved reviews-concurrency-before. Desktop, phone detail and inbox captures inspected. Buildreviews-build.log/TypeScript pass; current focused lint recorded.
- Full164migration chain and all assertions passed in disposable girlzculture_reviews_clean (reviews-clean-chain.log); subsequent narrow RPC current-queue projection reapplied only to that local function, SQL assertions passed reviews-database-projection.log. Synthetic fixtures initially violated real service-origin/overlap guards; corrected fixture styles/dates, no safeguards removed.
- Existing verify:review-media-reminders had stale static anchors for old dashboard title, old inline Admin projection constants and pre-sort public catalog columns. Updated it to check current required heading/component and exact existing projection constants, retaining forbidden-field and original behavioral checks. reviews-existing-final.log passed. No Admin application change.
- Not released; no redesign push/PR/remoteCI/prod schema/data/config writes. Both25-dollar caps unchanged. Next: Messages composition, retained history and24h active window, two-way DeepL, consent-aware communications; then remaining waitlist/advisor/finance/detail/subscription and full140release evidence.

## Messages workspace and enforced conversation lifetime

- Prior Reviews commitde61274. Current Messages work is local, no remote PR/push/CI/production changes. BothUSD25caps unchanged. Latest fresh local databasegirlzculture_messages_release, localhost55432.
- New inbox: original names/text, URL search and All/Unread/Active/Closed categories, desktop conversation beside inbox, phone list/detail navigation, grouped booking/policy context, exact failed-send incident and preserved draft/requestID. Drafts live only in memory, separate per conversation; actor change clears them. Opening phone inbox does not mark its hidden first conversation read. Explicit source/translation controls exposeEN/FR/ES/zhCN; legacywo fixture coverage is not provider acceptance.
- Reproduced route gaps: expired/cancelled sends accepted, assigned staff could access another professional conversation, support could POST despite read-only UI. Fixed scope before queries/translation, direct DB inserts and role checks. Pagination replaces silent100booking/1000message cap. Closed history stays readable; current confirmed end+24h deadline, accepted reschedule changes window, pending date does not, cancellation closes immediately. Existing marketplace guest secure-management notifications retained; manual unlinked guests remain blocked.
- Pending migration20260918150923 (165): serialized send guard and fresh canonical identity/role/assignment checks, direct-table/eventRLS with matching boundaries, replay acknowledgment after closure, no history deletion. Reproduced in-app preview leak to unassigned staff with actual SQL; fixed in-app trigger and outgoing push recipient lookup using the same scoped helper. Two businesses sharing a customer tested under real authenticated RLS; revoked staff see no history. Assistant closed proposal now rejected before save; DBguard also protects final confirmation.
- Evidence ../redesign-evidence/messages-boundary-before.log; messages-notification-before.log; messages-assistant-before.log preserve original failures. messages-core-verified.log51/51passes. messages-notification-final.log and messages-remaining-database.log pass. Full165clean chain/allassertions messages-clean-verified.log passes. Earlier clean replay exposed over-restriction of established marketplace guests (corrected) and omitted Engine migration-head metadata (added). No production repair/migration.
- Browser first failure fixture usedreference instead of actual request_id; old nested-details locator selected outer context and support heading became nonunique. Corrected fixture/precise selectors, retained outcomes/accessibility checks. WebKit intercept failed while service worker active; matching established serviceWorkers:block fixture setup fixed allfournewcases, messages-webkit-fixture.log. No application auth changes or arbitrary waits/retries. Originalstraces under messages-first-browser/messages-corrected-browser; successfulscreens under messages-webkit-passed. Spanish desktop and Englishphone visually reviewed; unreadlabeltranslation and soft context borders corrected afterward.
- Latestproductionbuildmessages-mobile-read-build.log passed; TypeScriptmessages-types-mobile-read.log passed; final lint before tiny mobile-read guard passed0/0(messages-lint-verified.log). Final16-casebrowser matrix now running in session11407/messages-browser-final.log: 8new four-locale cases+6existingcustomer/team/support welcome/policy/original/a11y cases+2composer cases. Do not repeat unchanged checks; finish this run and commit once passed.
- Remaining140scope continues. Next communications: existing createCustomerApprovedReschedule already supports professional-specific proposed slots, customer approval, unchanged prices and availability verification; connect stylist substitution through this workflow instead of second system. Owner controls embeddedOwnerDashboardApp~3030; publicGuestBookingManager supportsaccept/decline. Inspect detailed consent/reminder/rebooking/campaign requirements before adding needed code. Page09/COMMS02markedIN_PROGRESS/AUTOMATEDONLYwithremainingclauses, notcomplete/live. Also outstandingwaitlist/advisor/financeofflineclients/dailyclose/details/subscriptionrootcause/remainingpages/full140proof/protectedrelease.
- FinalMessagesbrowserresult:16/16passed in messages-browser-final.log (Chromium/WebKit fourlocales/viewports+existingrecipientrole/a11y+composer). Latestsourcebuildmessages-mobile-read-build.log/typespass. Originalcomposerfixture logs local AUTHENTICATION_PROVIDER_UNAVAILABLE from background fixture traffic; its assertion passes, not claimed a liveauth/providercheck. No productionrequests. FinalreadReceipt guard verified: phone inbox leaves unopenedconversationunread, openingmarksitread. No unchangedrerun needed.

## Customer-approved professional substitution

- Messages foundation committed c615393. New substitution module continues existing proposal/secure customer approval flow; no second booking system. Dashboard explicitly offers another professional at the same time or date-based alternatives; original professional and booked facts remain visible. Owner draft/reference/retry preserves data. EN/FR/ES/zhCN copy and date formatting; customer may accept/decline; live option failure preserves selection; expiry closes response controls using the actual deadline.
- Reproduced recalculation from current service duration instead of booked duration, missing same-time substitution checks, invalid booked duration acceptance (reschedule-before.log), and duplicate proposal creation in actual local SQL (reschedule-retry-before.log). Correction supplies trusted booked duration/buffer to availability, checks same time/different professional, and deduplicates explicit request IDs. Fresh actor/business/assigned-role and offered professional service eligibility checks run inside the locked transaction. Appointment snapshot prevents stale acceptance; current assignment/conflict rechecked at acceptance, money unchanged. SQL tests both businesses, forged actor and unassigned professional; no cross-business data or flags.
- Migration166 /20260918160921 also reuses the current valid signed management token for communication retries (only hashes persisted, serialized and audited), uses existing channel delivery claims for proposal retries, and makes accepted-proposal confirmations distinct from original confirmation events. Full secure token rotation for customer security actions remains. Existing Admin proposal calls remain supported through current admin bookings permission, no Admin UI changes.
- Local evidence: server5/5 substitution-server-final.log; availability5/5 subsection substitution-core-final.log; existing secure guest/communications/reschedule verifiers pass. Browser18/18 substitution-browser-final.log:14 substitution/consent/expiry cases+4 public assignment cases in Chromium/WebKit. Four locales/viewports, price/duration preservation, failed draft/ref/sameID and reload, customer accept/decline/conflict and virtual deadline (no arbitrary sleep). Initial12cases passed; visual review found missing current professional label, fixed and asserted. Desktop/phone captures examined. Buildsubstitution-verified-build.log and focused lintsubstitution-lint-corrected.log pass; earlier render Date.now purity failure corrected with state and actual deadline timer.
- Full166migration chain and all assertions passed on fresh girlzculture_substitution_release (substitution-clean-final.log). Initial chain caught accidental SQL quote corruption in newly generated migration constraint; original failure retained substitution-clean-verified.log, corrected migration verified fresh. Later issuance audit addition was reapplied only to its local function; substitution-audit-final.log all assertions pass. No production repair/migration/data/config writes. Required CI later covers final complete source.
- Production still previous release; no redesign push/PR/remoteCI/deploy. BothUSD25caps unchanged. Live substitution acceptance not claimed; no real email/SMS/push/customer booking changes. Reuse provider/language/calendar evidence unless later directly affected.
- Next: COMMS03 reminders and postvisit/rebooking, then consent-aware own-client communications/WAIT01/ADVISE/remainingfinance/details/subscription/full140protectedrelease. Reminder inspection found booking_reminder_claims keys only bookingID+hours and delivery key lacks appointment revision; accepted reschedule can suppress future reminders. No customer notification/marketing preference implementation found beyond business settings notification_preferences; CustomerAccount.tsx settings currently name/email/MFA only. Must add appropriate consent/preferences in existing account/secure booking areas, no implicit marketing consent or actual campaign sends. Existing protected scheduler src/app/api/bookings/reminders/route.ts calls processBookingReminders in supabaseAdmin.ts. Keep existing worker/permission flow.

## Reminder lifecycle correction and progress report

- Founder requested immediate140-row report; remote main/production rechecked2026-09-18T17:13:45Z atd37cbf4/deploy6aac503fa5eacb00089c6d87locked. 13localcommits atf2b27e5 had not been pushed. Normalized conflicting tracker status/state fields:1completed(EXEC03decisions),17written/unverified,102inprogress,20requirement-specificnotstarted,0externalblockers. Detailed snapshot PROGRESS-2026-09-18.md; local synthetic gallery../redesign-evidence/progress-2026-09-18/index.html. Screenshot review exposed mobile substitution header overlap and untranslated Unread label, still pending final layout/copy corrections.
- Reminder bug reproduced actualSQL: completion keyedonlybooking+hours suppressed reminders afterreschedule (reminder-reschedule-before.log). Pending167migration20260918165227 adds schedule revisions, revisioned claims, final serialized cancelled/stale delivery gate, bounded duequeue excluding completed/leased/terminal rows before250limit, legacycompletion guard, service-role-only RPCs. Oldhistory retained, realprovider sends neverperformed.
- Newworker passes selectedrevision throughclaim/delivery/complete/fail; status/revisionreadback; secureguestlinkreuse; stableemailidempotencykeys. Builtin4localeremindercopy/dates withEnginepublishedpriority. Newintegrationtest then exposed shared translated emailrender removingvariables before substitution; corrected withoutdouble-substituting literal facts. Original failurelogs retained.
- Focused17/17 testsreminder-server-final.log; testsrenamedp0-business-reminders andp0-business-reschedule toensure requiredtest:p0:core includes them. Testloader nowhandles existingmjs files. TypeScript/lint/build andexistingreminderverifierpass. Freshgirlzculture_reminder_release full167chain/assertions pass(reminder-clean-final.log). SQLfixture initialmissingownerrole/overlappingappointments corrected; no appprotections relaxed. No productionmigration/data/config/capchanges.
- Next:publish current workasDraftPR for visibility, thenfinish COMMS03/04preferences/postvisit/campaign, WAIT/ADVISEandremainingpage/detail/finance/billing/public/full140release. No newgeneralpermissionneeded.

## Communication controls and CI correction checkpoint — 18 September

Draft PR #80 is open: https://github.com/girlzculture/girlzculture/pull/80. No redesign release is live. Full 140-item status is in PROGRESS-2026-09-18.md and requirements.json: 1 governance decision completed, 17 implemented/unverified, 103 in progress, 19 not started, no external blockers.

Customer-controlled own-business preferences, guest booking scope, audit/revision/idempotency, default-off optional messages, signed explicit opt-out and fresh per-channel SQL reservation guards are implemented. All168 migrations pass from an empty local PostgreSQL database; 10 preference browser cases pass in four languages. Post-visit automatic delivery and owner campaign workflow remain unfinished.

Required CI at e6d6936 failed; original logs remain under work/redesign-evidence. Local fixes now pass design-system gates, four-language inventory (2386 each), session regressions, exact incident-copy handling, 12 calendar pointer cases across three engines/four viewports, assistant conversation controls and public keyboard navigation. Finance/localization and Spanish phone route audit:10/10. Wolof UI acceptance is explicitly deferred; legacy functional tests remain and its648 missing sources are still reported. TypeScript/build pass; full lint has13warnings/0errors; final changed-file lint is clean. No production mutation, provider calls, customer notifications or cap changes.
