# Business money insights — implementation and evidence

Status: AUTOMATED ONLY / IN PROGRESS. Not merged, hosted, or released. SF-MONEY remains partial.

The existing protected finance read supplies the same canonical books used by Finances. A deterministic helper calculates an equal-calendar-day comparison in the business timezone, completed gross sales, net receipts dated in the period, pending recorded value, cumulative unpaid balances as of period end, and up to two explained actions. Deposits remain part of receipts. No provider/model calls or financial writes occur.

Only the full-business finance scope receives these insights from the server or sees the panel. A professional-only ledger is not relabelled as business-wide advice. Every book row is validated against the authenticated business before calculations, including rows excluded from totals. Service price samples preserve their recorded service labels and report period, record/unit counts, unit range/median and discounts. They are explicitly this business's own records, not geographic market benchmarks or recommended new prices. A record is not a unique client.

Focused evidence: `money-insights-core-final.log` 8/8 tests, covering DST, zero comparison baseline, separate sale/receipt/balance dimensions, service samples, future and foreign rows, full/own server scope and four-language key/placeholder parity. `money-insights-core.log` includes the six calculation cases plus15 existing finance-data/route cases,21/21 passed. Lint passes. Current typecheck passed before later parallel source additions. The first attempted test process was blocked by subprocess permissions; it is not product-failure evidence. The subsequent one failed comparison used a cross-VM object prototype; structured values were identical and the assertion was corrected to compare serialized data.

New browser10 cases cover four languages and mobile/tablet/desktop/landscape, actual amounts, selected-period links and professional-only exclusion. All10 now have passing Chromium/WebKit evidence:8 in the initial combined run and the two corrected Spanish cases in combined-179-focused-browser.log. The latter run also verifies EN/FR viewport captures and selected-period navigation. Exact numerical assertions remain. Desktop EN and phone FR captures were inspected; long French bottom-navigation labels wrap awkwardly and remain a shared mobile design follow-up, not a claimed complete layout acceptance.

The separate cancellation/no-show and attributed booking-offer slice now has14/14 composite browser evidence (BOOKING-MONEY-ACCEPTANCE.md). Still required: broader expected-versus-realized planning, capacity/opportunity analysis, full useful-action journeys, bounded hosted acceptance and release gates. Existing Morning Brief/calendar openings and team finance breakdown remain separate supporting features. Missing market or promotion evidence must not become invented advice. Both USD25 AI caps and business isolation remain unchanged.
