# Recorded finance rankings — 2026-09-19

Status: **AUTOMATED ONLY**. Code, protected reader/model regressions and focused local production-build browser evidence are available. Final required CI, hosted/model-provider behavior and complete visual acceptance are not established by this document.

The original FIN-06 / EXPENSE-01 gap is reproduced in `../redesign-evidence/finance-rankings-before-actual.log`: both assertions fail. The actual `readAssistantData` response has the highest professional among 15 and the largest expense category among 40; the actual planner/answer request loses them at the existing 32-property and 12-array bounds. Initial test harness shape mistakes are retained separately in `finance-rankings-before.log` and are not defect evidence.

`businessFinanceRankings.ts` now sorts the complete already-authorized canonical summary numerically before selecting twelve rows. It reports the complete total and amount, shown/excerpt counts, exact maximum, complete tied-leader count, and bounded tied names. Stable IDs/category strings determine tie order. USD and the selected inclusive local-date period remain explicit. No additional business, customer, provider or database query is introduced.

Professional earnings mean recorded commission earned (including reversals) plus wages due in that period. Gross service sales, recorded payouts, booth rent and payment receipts remain separate. Unassigned compensation is explicitly excluded from a professional winner. Negative and zero recorded values remain meaningful; an empty pool has no winner. Own-professional permission has only its canonical own scope and never establishes a business-wide winner. Operating expense ranking excludes inventory asset purchases; expense completeness remains unverified and recorded profit remains qualified.

`ownerReadServer.ts` supplies the ranks from `readBusinessFinances`; `gcAssistantPlanningServer.ts` packages existing scalar totals, receipt breakdowns and explicitly excerpted record lists within the unchanged model bounds. Fresh history authorization/readback continues in both planner and answer phases. `FinanceEarningsLeaders.tsx` is mounted only in the full-business Team panel of the current matching Finance snapshot. Its seven labels are registered in the existing English/French/Spanish/Simplified Chinese source catalog.

Verification:

- `finance-rankings-final.log`: **13/13 passed, zero skips**. Actual protected finance transport stub → real canonical transformation/aggregation → real assistant reader → real planner/answer projection. Includes full-pool maxima, >12 ties, empty versus zero, distinct compensation/payout/turnover, unassigned and inventory exclusions, own/mixed scope, changed and revoked history, nonfinite/overflow failures, local-midnight boundaries, negative refund compensation and required-locale copy.
- `finance-rankings-types.log`: full TypeScript exit 0. `finance-rankings-lint.log`: scoped ESLint exit 0.
- `finance-rankings-regressions.log`: **78/78 existing finance/core/data/assistant-planning cases passed, zero skips** (81.33 seconds).
- Independent read-only review by `reconciliation_review`: PASS; no concrete blocker. This is not hosted/model-provider acceptance.
- The five cases in `tests/browser/business-finance-tabs.spec.ts`, prefix `Business finance ranked team summary`, passed in both Chromium and WebKit: **10/10 ranking browser cases**. EN desktop, FR phone, ES tablet, zh-CN landscape and own-earnings-only staff preserve exact amount/tie counts, period, names and basis; they check empty-period transition, no horizontal overflow/accessibility and unexpected-request rejection. Evidence: `combined-192-focused-browser.log` (16/16 overall in that run, including six separate mobile-label cases; only ten are ranking cases).
- Local production build192 passed with identical before/after **836-file** source digest `d6e6ed7561fa7df4c73c2b38c773b83370f845b2d803570ac15b43859412ff64`, recorded in `combined-192-build.log` and `combined-192-source-{before,after}.json`. The root reviewer inspected the actual French390px and Chinese844px ranking screenshots: the reviewed content was readable. This is bounded viewport evidence, not whole-page visual or hosted acceptance; later source changes require their own verification boundary.

No migration, new assistant tool, provider request, payment mutation, production deployment or change to either $25 cap is part of this correction.

## Build194 original-data boundary and affected browser evidence

`finance-timezone-before.log` preserves the reproduced Spanish empty-team locale failure: its sole unexpected English item is the canonical `America/New_York` identifier. The correction marks only that original timezone and nonempty supplied professional names `translate="no"`. All headings, explanations, empty/unnamed fallbacks and excerpt labels continue through the existing translations; the locale helper, allowlist and its assertions are unchanged. Independent source review found no blocker. The ranking test additionally requires the exact original twelve names in their stable order and the timezone, plus their narrow original-content attributes.

On local production build194, **18/18 affected finance cases pass**: ten `Business finance ranked team summary` cases plus eight `Business finance tabs preserve real content, filters and repeated entry` cases, across Chromium and WebKit. The unchanged strict empty-team locale case that originally exposed the identifier issue is a separate acceptance check; these18 results are not represented as a rerun of that case. Evidence: `../redesign-evidence/combined-194-browser.log`. The full mixed run is **42 passed / 8 promotion failures** in3.3 minutes, not a complete run or release pass.

That separate original empty-team check subsequently passed **8/8 in 20.3 seconds** on build195: four languages in Chromium and WebKit, retaining the unchanged strict locale assertions, owner versus own-earnings-only staff actions, unavailable mutation controls and reporting-period focus. Evidence: `combined-195-finance-empty-browser.log`. Build195's837-file source digest is `33851b2787089932aaec1d08e8842dc14cd391089d9fa341e64cf75149206b12` in both source manifests. These eight are additional to, not included in, the build194 eighteen.

Build194 passed with identical837-file before/after digest `3b2e71fc92faceea8a6ce4ba39f51424d029581b627f89daff25855afca3e7ab` (`combined-194-build.log`, `combined-194-source-{before,after}.json`). Subsequent compiled edits are limited to three promotion option-value attributes, outside this finance source.

Two actual WebKit viewports were directly inspected:

- [French390](../../../redesign-evidence/combined-194-browser-results/business-finance-tabs-Busi-ac3eb-ected-period-evidence-in-fr-webkit/finance-earned-compensation-ranking.png): translated earnings basis, $990,13 tied leaders, all12 original names and12/13 excerpt label are readable. The exact `2026-09-01 — 2026-09-30`, `America/New_York` andUSD remain visible.
- [Chinese844 landscape](../../../redesign-evidence/combined-194-browser-results/business-finance-tabs-Busi-8882c-ed-period-evidence-in-zh-CN-webkit/finance-earned-compensation-ranking.png): translated basis and tied count, original timezone/period, $990 and the first original names are readable. Remaining names extend below the short viewport; this capture does not show the entire list simultaneously or establish the initial whole-workspace composition. The heading above the ranking is partly outside the scrolled view, not proof of a new ranking defect.

No new changed-area defect was established by those images. The local fixtures do not establish native-speaker review, OS keyboard behavior, real business amounts, hosted authorization, model/provider answers or live release. No additional test or provider call was run for this documentation review.

## Ordinary Team-card original names — reproduced before;8/8 after

A separate VALUE-03 source review found that ordinary Team cards did not yet protect original professional names, although the ranking list did. The new existing-spec regression targets those direct cards, excluding `FinanceEarningsLeaders`. On frozen build195, the French WebKit case reproduced the defect: the professional's saved name `Save` displayed as `Enregistrer` alongside the unchanged $990 wage. The separate unassigned record correctly displayed `Non attribué` and $100 service sales. Evidence: `finance-original-name-before.log` and the directly inspected [French before viewport](../../../redesign-evidence/finance-original-name-before-results/business-finance-tabs-Busi-5f0f5-ginal-collision-names-in-fr-webkit/finance-original-name-fr.png); the original failing trace is retained beside it.

The correction changes only the ordinary card's supplied `names.get(id)` value to a `translate="no"` span. The `Unassigned` fallback, financial labels and formatting remain translated. No other raw team-name output occurs in this component's render; the separate ranking already has its boundary. No calculation, data, permission, action or provider behavior changes.

Four new cases, `Business finance ordinary team cards preserve original collision names`, cover English desktop, French phone, Spanish tablet and Chinese landscape. They require the exact original name, translated fallback and wage label, unchanged $990, repeated tab entry and reload, and no mutation/unexpected request. Their build196 after-check now passes **8/8 across Chromium and WebKit**, within the **14/14** mixed Finance/promotion run in `combined-196-finance-offers-browser.log` (58.2 seconds). The earlier18 passing build194 cases are not reused as proof of this later correction. Independent source review and focused ESLint passed.

Build196 passed with the identical837-file before/after digest `bceadfcc11592754784778c76e350c02bc6393b0fe2b11bee6e101f89840bcec` (`combined-196-build.log`, `combined-196-source-{before,after}.json`). The actual [French390 after viewport](../../../redesign-evidence/combined-196-finance-offers-results/business-finance-tabs-Busi-5f0f5-ginal-collision-names-in-fr-webkit/finance-original-name-fr.png) now visibly preserves `Save`, while `Non attribué`, the financial labels and the $990 wage remain correct. The actual [Chinese844 landscape after viewport](../../../redesign-evidence/combined-196-finance-offers-results/business-finance-tabs-Busi-2c348-al-collision-names-in-zh-CN-webkit/finance-original-name-zh-CN.png) likewise preserves `Save`, the translated `未分配` fallback, all six financial labels andUS$990.00; both ordinary cards fit the inspected scrolled view without changed-area clipping. The French BEFORE image remains retained above.

These are bounded fixture and rendered-state results. They do not establish all Finance workflows, native-device keyboard behavior, hosted permissions/persistence, provider acceptance or production release. No additional test, browser or provider operation was launched for this screenshot/documentation review.
