# Offer date roundtrip and save recovery

Status: **AUTOMATED ONLY — seven focused Node checks and all ten unique promotion browser cases have passing evidence. The eight corrected save/recovery journeys passed on build196; the two no-write DST validation cases passed on build195 with unchanged promotion application source. Scoped lint passes. Original failures remain below. Hosted acceptance and full visual acceptance remain separate.**

The saved offer's declared time zone now controls both its local date fields and conversion back to UTC. Editing an unrelated field retains the exact original start/end instants, including seconds and either occurrence of an existing repeated daylight-saving hour. New invalid/nonexistent or ambiguous local times fail visibly before a save request; the owner can choose another time. An end must follow its start. Open-ended offers remain supported.

The editor no longer accesses the React event after the asynchronous save or resets the form after saving. The authoritative saved row remains open. A failed save retains the draft. Discount, target, activation/status and authorization behavior are unchanged; no backend or provider change was made.

## Reproduced before correction

Outside-Git `redesign-evidence/promotion-editor-source-probe.mjs` extracts the actual helper, serialization expression and submit function from `SalonPromotionsManager.tsx`, plus the installed React event dispatcher. `promotion-editor-source-before.json` records source SHA256 `76bfe4b45f2a531da0590770e47614e7f0b9967e48272c248185a2d4f00e2ac2`.

- An untouched `2026-07-01T14:00Z` became `18:00Z` on a New York device; winter shifted five hours. Paris shifted minus two/minus one hours and Shanghai minus eight. UTC was the unchanged control. UTC formatting and device-local parsing had been mixed.
- With the real dispatch lifecycle and a controlled delayed successful save, `event.currentTarget.reset()` threw `TypeError: Cannot read properties of null (reading 'reset')` after the row and route had already updated. This was a post-save error, not evidence that persistence failed.

This is isolated actual-source evidence with stubbed form/save/router dependencies, not a browser or provider run.

## After correction

- `tests/p0-promotion-dates.test.mjs`: **7/7 PASS**, no skipped cases; `redesign-evidence/promotion-dates-node-after.log`. Covers four device zones and two seasons, exact saved milliseconds, declared-zone changes, blank bounds, invalid dates/zones, nonexistent/repeated DST hours, both saved repeated-hour occurrences, reversed ranges, actual editor delayed success/failure and no-write validation, and four-language message parity.
- Scoped ESLint: **PASS**, `redesign-evidence/promotion-dates-lint.log`.
- `tests/browser/business-marketing.spec.ts`, prefix **Business marketing offer dates**: five cases in each configured Chromium/WebKit project. Four locale/device layouts cover owner and authorized staff editing, delayed failed save, retained draft, retry, reload, exact UTC instants and unchanged discount/status, then an explicit declared-zone edit. They also save/reload all three canonical statuses with exact activation/pause semantics and unchanged offer terms. The fifth verifies nonexistent/ambiguous selections make no save request. No skips, weakened assertions or configuration changes were added.

## Original browser findings and recovery

- `combined-194-browser.log`: **42 passed, 8 failed** across the 50-case mixed-feature run. All eight failures were the four promotion save/recovery cases in both engines. The English retry hit the existing dismissible failure notice covering Save; the test now asserts the exact failure reference and retained draft, dismisses that notice through its normal control, then retries. French, Spanish and Chinese exposed a real existing editor bug: options without explicit `value` submitted translated status text (`Brouillon`, `Borrador`, `草稿`) instead of `Draft`. The editor now gives the three localized labels their canonical `Draft` / `Active` / `Paused` values. Original strict payload assertions remain; added cases assert all three states and their unchanged `is_active`/`paused_at` behavior. Original traces/screenshots remain in `combined-194-browser-results`.
- `combined-195-promotion-browser.log`: **9 passed, 1 failed**. Every offer save/status assertion passed; English WebKit retained five same-origin RSC page errors during a test-driven hard reload. The trace shows reload starting at **23540.148 ms**, just before the post-save same-route RSC request at **23541.103 ms**. That request has status/time `-1`, while the surrounding 25 shell-prefetch responses are HTTP200. The interrupted route stream fell back to document navigation; errors followed at 23555–23558 ms. This was an early test reload after observing fixture mutation, not evidence of CORS rejection or failed offer persistence. The trace and readable timeline are retained in `combined-195-promotion-results` and `promotion-195-reload-timeline.md`.
- The first response-gate attempt additionally required `response.finished() === null`. `combined-195-promotion-complete-browser.log` retains **4 WebKit passes / 4 Chromium failures**: Chromium matched the expected HTTP200 component response, then timed out waiting for its streaming body to close. The English trace records the matched response at7985.359 ms, wait9.757 ms, receive `-1`, and no completed body. Thus the predicate was not the failure. Installed Next code decodes Flight streams rather than requiring a whole HTTP body to finish; its internal partial-stream helpers can also deliberately remain open. Those internal helpers alone do not identify this HTTP stream's precise retention cause.
- The corrected test arms the same exact own-route, non-prefetch response gate **before each successful Save**, requires HTTP200 and `text/x-component`, and verifies the visible saved confirmation and existing rendered/record assertions before hard reload. It no longer requires the streaming response body to close. Failed saves do not require navigation. All page-error, payload, unchanged-term, failed-save and reload assertions remain. There is no arbitrary sleep, retry, error filtering, prefetch disabling or application change for this correction.

## Final browser evidence and visual limits

- Build196 passed with **837 source files**, source digest `bceadfcc11592754784778c76e350c02bc6393b0fe2b11bee6e101f89840bcec`. Its only compiled change after build195 was the ordinary Finance card's original-name span; the promotion application source was unchanged.
- [combined-196-promotion-en-browser.log](../../../redesign-evidence/combined-196-promotion-en-browser.log): **2/2 PASS**, English save/recovery in Chromium and WebKit, 14.5 seconds.
- [combined-196-finance-offers-browser.log](../../../redesign-evidence/combined-196-finance-offers-browser.log): **14/14 PASS**, comprising the remaining **6/6** French/Spanish/Chinese promotion save/recovery cases and **8/8** ordinary Finance original-name cases, 58.2 seconds.
- Promotion evidence is therefore **8/8 corrected save/recovery journeys on build196**, plus **2/2 no-write DST validation cases on build195**: **10/10 unique cases with passing composite evidence**, not a claim that the original build195 command passed or that one final ten-case command was run. No final case failed or was skipped in the two build196 commands.

The four actual WebKit viewport captures were inspected: `offer-dates-en-390.png` in `combined-196-promotion-en-results`, and `offer-dates-fr-768.png`, `offer-dates-es-1440.png`, `offer-dates-zh-CN-844.png` in `combined-196-finance-offers-results`. All show readable saved start/end values and `America/New_York`; the French capture also shows the lower form and save controls without overlap. This is bounded evidence at the captured scroll positions. The English Starts label and Chinese start/end labels are partly behind the fixed header, and the short Chinese landscape viewport excludes the lower form. In the Spanish desktop capture, the native offer-type and Applies-to selections truncate their visible text within the four-column editor. These captures do **not** establish complete-form, all-control or whole-page visual acceptance; those visible limits remain recorded.

## Follow-up selected-label readability correction

The Spanish selected-label clipping above is a remaining readability defect, not a visual acceptance waiver. Root reproduced it on the unchanged build196 with the new geometry case: [promotion-select-before.log](../../../redesign-evidence/promotion-select-before.log), **1/1 failed**, Spanish1440 WebKit with the real assistant dock retained. The longest Applies-to choice measured **224.5748px** against **144px** of usable native-select width. Several other choices also failed; control height and page overflow were not the cause. Original trace, both select captures and per-option geometry remain in `promotion-select-before-results`.

The bounded correction changes only responsive layout in `SalonPromotionsManager.tsx`: Offer type and Applies to span two columns in the four-column desktop grid. Below640px the editor uses more of the existing page gutter, and these two selects use8px horizontal padding. The existing13px font floor and44px control height, assistant dock, complete translated options, values, field handlers and all offer calculations remain unchanged. The narrow-phone spacing is necessary too: the shipped Montserrat-Medium font measures the longest French choice at239.954px at13px, while the former320px single-column layout left only about200px usable. The revised width budget is about248px; this calculation does not replace actual browser evidence.

The appended **Business marketing offer selections** cases cover all five offer types and six scopes in all four locales at320,390,768 and1440px (16 layouts per engine). They retain the dock on desktop, measure every translated selected label with the current font and native-arrow allowance, require the existing13px/44px floors and no page overflow, preserve amount/status and original fixture records, and assert no write or unexpected request. The original save/date regressions are unchanged. **AFTER browser execution and screenshot inspection are pending; the visual defect is not yet accepted as resolved.**

Build197 subsequently passed:842 source files, identical before/after digest `219252354eb1690197aceb2ccc595526c797b03c73068493da982ee590ead492`. Its `combined-197-replies-offers-browser.log` contains **32/32 passing offer-selected-label cases**, all 16 locale/width combinations in both Chromium and WebKit. Each of all 11 choices satisfies the unchanged measured-width/font/control-height/overflow/no-write checks. The command as a whole was **34 passed / 7 failed** because seven new reusable-reply tests had separate locator failures; it must not be described as an all-pass command. Those reply failures were corrected and independently verified on the same compiled application as recorded in `REUSABLE-REPLIES-ACCEPTANCE.md`.

Root inspected `offer-select-scope-es-1440.png` and `offer-select-scope-fr-320.png` from the actual WebKit results. The Spanish desktop scope label is fully visible beside the retained dock; the longest French scope is fully visible on 320px with its native arrow. The prior clipped values are resolved in these captures and all measured combinations. This does not replace other whole-page/editor/native-keyboard acceptance.

Integrated release gates and hosted persistence remain separate. Fixture reload evidence must not be described as real database persistence or actual cross-device login acceptance.
