# Default workspace marketing fixture correction

Status: **automated local correction verified on production build; required CI pending**. No production route or assertion was weakened.

Automatic release run `35432248743`, shard 2 job `105868815130`, at checkpoint `c0fc677`, failed 24 route-inventory/populated-route cases. The original log is preserved outside Git at `../redesign-evidence/ci-c0fc677-release-shard2.log`; it records 321 passing cases and four skipped cases. Every failing case recorded an unhandled `GET /api/salon/marketing`. That exact failure prevented the later language/reflow/accessibility aggregate assertion from being reached.

The existing required run [`35432248630`](https://github.com/girlzculture/girlzculture/actions/runs/35432248630), job `105868814672`, completed with the same failure: 48 failed route audits (24 Chromium and 24 WebKit), 993 passed and five skipped in its 46.9-minute main browser step. All 48 recorded the identical missing marketing GET fixture. The complete empty-database migration chain, TypeScript, lint, production build, media checks and earlier focused browser steps passed. The final status and original complete log were retrieved once and preserved as `ci-c0fc677-required-final-status.json` and `ci-c0fc677-required-final.log`; this workflow was not rerun. This is evidence for commit `c0fc677103b8999863d6d009939bcdb72c5842d8`, not acceptance of subsequent uncommitted changes.

The existing production marketing route was already covered by dedicated feature fixtures, but the shared default workspace fixture lacked its GET response. The shared fixture now returns the route's actual contract: an empty post collection, this business's timezone, external posting disabled, own visible gallery metadata, own non-draft/unarchived service selections, own active offers and own completed-service references. POST and any other unknown request remain failures. No real publication, provider request or business record was changed.

The focused `Business marketing default workspace route audit` cases exercise only the affected Promotions route, using empty and populated workspaces in English, French, Spanish and Simplified Chinese at 390, 768 and 1440 pixels. They assert the exact GET contract, current-language interface, no horizontal overflow, unchanged WCAG A/AA Axe checks, no unaccounted requests and no mutations.

`../redesign-evidence/marketing-fixture-audit.log`: **24/24 passed in 1.0 minute on the local development server**. The subsequent `marketing-ci-corrected-production.log` passes **48/48 in2.8minutes**, Chromium and WebKit on the combined corrected production build. Targeted ESLint passed in `schedule-marketing-fixture-lint.log`. The next changed-source required CI run remains pending; the failed unchanged workflow was not rerun.
