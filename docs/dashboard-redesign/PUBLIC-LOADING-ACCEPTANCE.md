# Public marketplace loading layout

Status: locally verified component correction, not deployed or full public-site acceptance.

The existing nearby and featured home sections rendered loading cards in a vertical stack on narrow phones, then replaced the stack with a horizontal carousel. At 390px the reproduced loading card positions spanned 901.5px vertically; the regression expected a single row and failed on the prior production build. The original evidence is retained in `../redesign-evidence/public-loading-before.log` and its report.

`SalonCardSkeletons` now shares the actual compact card widths and image ratios. Home loading rows stay horizontal and decorative overflow is clipped. The existing featured-results page retains its full responsive grid. Section order, root landing, hero, imagery, labels, routing, discovery data, location and business facts are unchanged. No reference-image slogans or sample claims were introduced.

| Check | Evidence |
| --- | --- |
| Chromium: 6 passed | `../redesign-evidence/public-loading-after.log` |
| WebKit: 6 passed | `../redesign-evidence/public-loading-webkit.log` |
| Targeted ESLint | `../redesign-evidence/public-loading-lint.log` |
| Design source guard | `../redesign-evidence/public-loading-design.log` |

The browser cases gate real interface fetches with synthetic discovery responses. They cover 320, 390, 768 and 1440px; loading-to-result width consistency; no horizontal page overflow; real profile/booking destination construction; preserved fixture prices; the featured results grid; explicit error recovery; and honest empty states without invented cards. There are no new test retries or arbitrary sleeps. Both engines ran against the local development server after the correction, not a held production candidate. The before test ran against the prior local production build.

Loading and loaded390px captures were inspected in Chromium and WebKit. The local preview document links the images. The development indicator in those initial captures is not production UI. The subsequent corrected production build passes all12 cases within `combined-181-corrected-browser.log` (54total); its WebKit390px loaded capture was also inspected and has no development indicator. These are synthetic local discovery records, not a live-site acceptance claim.

This supplies bounded evidence for UI-07 and PUBLIC-10/12/13/16. Full translated public-page review, keyboard/booking journeys, final required CI on the combined source and hosted/public acceptance remain separate requirements. It does not mark those whole groups complete.

## Initial and changed-location pending state

Status: **AUTOMATED ONLY — 18 source regressions and all12 compiled Chromium/WebKit cases pass; hosted acceptance pending.** The held merged candidate exposed a separate existing state-lifecycle gap. Both placements initialized with empty rows and `loading:false`; after a valid location became ready, the existing80ms timer had not yet called `load()`. The intervening render therefore displayed the nearby empty result or featured promotional fallback without a completed discovery response. The same issue retained old rows, errors or featured copy when the query changed. The d37-to-merged-source comparison confirms the earlier loading-layout correction changed skeleton presentation, not these state transitions. This is not evidence of an authoritative empty database result or of missing public rows.

Both placements now bind their completed snapshot and pending state to the effective latitude, longitude, radius and result limit; Featured also binds carousel versus full-grid mode. An unread query immediately renders the existing skeleton before effects or the delayed request. Rows, totals, errors and featured copy from another query are not displayed. Request generations guard success, failure and completion, including manual retry and Featured append, so obsolete transport cannot replace current results or clear another request's pending state. Same-query append retains its cards, ID deduplication, total and rotation seed. Actual empty responses, error references/retry, location-needed states and label-only location changes retain their existing behavior. Discovery eligibility/arithmetic, location permission behavior, timing, routes and providers are unchanged.

- **FAIL, original source reproduced:** `../redesign-evidence/public-discovery-pending-before-corrected-harness.log`:16 cases,3 passed/13 failed. Tests execute the actual TSX placements and API response parser with separate controlled render, passive-effect,80ms timer and transport boundaries. The earlier `public-discovery-pending-before.log` is retained but is not valid full-suite evidence: its default-export component stubs had the wrong module shape; that harness issue was corrected before the recorded3/13 reproduction and before any application edit.
- **PASS, bounded source behavior:** `public-discovery-pending-after.log`: the same16 cases passed. `public-discovery-pending-final.log`:18/18 passed,0 failed/skipped, with additional effective-limit/full-grid and invalid-coordinate coverage. After the equivalent stable-object request-counter cleanup, `public-discovery-pending-counter-final.log` again passes18/18 with0 failed/skipped. No browser, live network, elapsed-time sleep or automatic retry is used. This hook scheduler is source-level behavior evidence, not actual React hydration/rendered-browser acceptance.
- **PASS:** final scoped ESLint exits0 with0 warnings in `public-discovery-pending-counter-lint.log`. The intermediate two cleanup-ref warnings are retained in `public-discovery-pending-lint-final.log`; no lint rule was suppressed to resolve them. The initial whole-tree TypeScript attempt in `public-discovery-pending-types.log` stopped on a malformed pre-existing generated `.next/dev/types/validator.ts` at line323, before application type checking. Root preserved that stale generated directory outside the repository in `public-loading-stale-dev-types` for fresh build regeneration; no manual generated-type correction or application type-check pass is claimed.
- **PASS, compiled browser behavior:** the four existing width cases in `tests/browser/public-loading-layout.spec.ts` now hold the actual80ms placement timers and assert both skeletons before any discovery request, then retain every previous loading geometry/card-width/link/price assertion. They also change the canonical stored location, assert pending with no previous-area cards before the next timer, and release the requests to verify the exact replacement records. The existing genuine-empty/error-retry and full Featured-grid cases remain. All12 cases pass in38.0seconds across Chromium/WebKit against the completed local production build (`public-loading-final-browser.log` and `public-loading-final-browser-results`). The390px WebKit loading/loaded screenshots were directly inspected: compact cards, correct fixture prices and no false empty notice; these are synthetic fixture records.

The existing first-relevant native-location request and its timeout message are outside this correction. The browser fixture explicitly records an already-handled native request so its stored-area checks do not invoke that separate workflow. No production data, account, provider configuration or notification changed. Independent final source/test review, including the stable-object counter cleanup and saved18/18/lint evidence, passed with no concrete lifecycle blocker. `public-loading-typed-build.log` records the successful production build, including TypeScript. The original sandbox spawn EPERM and subsequent test-only timer-overload type failure are preserved in `public-loading-final-build.log` and `public-loading-final-build-elevated.log`; the latter was corrected and whole-tree TypeScript passed before the final build. Remote exact-source CI and real hosted acceptance remain separate.

The first parent-coordinated elevated build compiled application source, then failed TypeScript on the browser fixture's `clearTimeout` wrapper: its number-only parameter did not cover the repository's combined DOM/Node overload. The original `public-loading-final-build-elevated.log` is retained. The wrapper now derives its parameter from `Parameters<typeof window.clearTimeout>[0]`, consumes only numeric held IDs and forwards all others unchanged. There is no `any`, suppression, application change or assertion change. Whole-tree `npx tsc --noEmit` then passed before scoped ESLint, which also passed with0 warnings (`public-loading-timer-wrapper-types.log`, `public-loading-timer-wrapper-lint.log`). Independent review of this test-only delta passed. The successful type check is not a completed production-build or browser result.
