# Manual configuration handoff

Prepared 19 September 2026 from the existing source, recorded configuration evidence and the latest founder instruction (`32cd5043-31aa-4165-975b-53cd476f7cea`, sections 2, 8, 9 and 11). No account settings, credentials, provider resources or migrations were changed for this handoff. Values not established below must not be guessed. This document supersedes older activation instructions where they conflict with the founder's current pause.

## Stripe — paused

**Founder-directed pause—remaining Stripe configuration and live provider acceptance will be handled separately.**

There is no current Stripe action or credential request. Do not dispatch the sandbox workflow, enable its mutation flags, change keys/event destinations/prices, or continue provider troubleshooting. Preserve existing subscriptions, agreements, records, payment protections and completed code. Necessary unchanged regression coverage may remain in ordinary CI. Unrelated release work continues.

The original unavailable-method diagnosis is already recorded: the saved customer/subscription belong to the identified Girlz Culture sandbox, while the failing production request used the live account. Creating a replacement subscription or changing the production secret to a sandbox key is not a remedy. Completed application work includes owned setup Sessions, durable attempts, default-only application/reconciliation, masked readback and scheduled-agreement guards. Local simulated-provider/database/browser evidence is not successful hosted Stripe acceptance. See [billing verification](BILLING-SANDBOX-VERIFICATION.md) and [payment-method acceptance](SUBSCRIPTION-PAYMENT-METHOD-ACCEPTANCE.md).

The following is a reference for the separate future Stripe work, **not an instruction to perform it during the pause**:

| Service / exact settings location | Field and established value | Secure location / later expected signal |
| --- | --- | --- |
| GitHub `girlzculture/girlzculture` → Settings → Environments → `billing-sandbox` | Existing required repository-owner reviewer and reviewed-branch restriction; preserve them. | This protected environment already exists. A workflow approval is not provider acceptance. |
| Same environment → Environment secrets | `STRIPE_SANDBOX_SECRET_KEY` — reuse the existing identified sandbox's usable secret/restricted **test** key; no value belongs in this document. Last recorded inspection at 20:47 UTC on 19 September found zero environment secrets. | If Stripe work resumes, enter directly in GitHub's secret field, never chat, Git, logs, command arguments or an artifact. No second credential request is being made. |
| Same environment → Environment variables | `BILLING_SANDBOX_ACCOUNT_ID`, `BILLING_SANDBOX_CUSTOMER_ID`, `BILLING_SANDBOX_SUBSCRIPTION_ID` — retain their already configured reviewed association; do not substitute newly created records. `BILLING_SANDBOX_APPROVED_SHA` — later exact reviewed commit, matching dispatch and checkout. | Reuse the saved values in that environment; raw association/provider objects are not copied to this public repository. Later read-only evidence must match all three identities and test mode. |
| Same environment → Environment variables | `BILLING_SANDBOX_AUTHORIZE_SETUP_CANCEL=false`; `BILLING_SANDBOX_EVENT_ROUTING_REVIEWED=false`. Workflow input `billing_sandbox_mode=off` remains the default. | Leave false/off throughout the pause. Available implemented modes are only `read-only` and `setup-cancel`; successful hosted saving is not an enabled verifier mode. |
| Stripe Dashboard → the already identified sandbox → Workbench → Webhooks / event destinations | Recorded destination `https://girlzculture.com/api/stripe/webhook`; subscribed events: `checkout.session.completed`, `customer.subscription.created`, `customer.subscription.deleted`, `customer.subscription.updated`. | Later isolated acceptance must prevent these test events reaching production and review organization destinations, EventBridge and CLI forwarding too. Nine recorded failed deliveries do not prove isolation. No destination change is requested now. |
| Existing setup-cancel harness return settings | `http://127.0.0.1:3199/salon/dashboard/subscription?payment_method_session={CHECKOUT_SESSION_ID}`; cancel uses `?payment_method_cancel=<attempt UUID>`. | These are the existing harness's loopback return values, not a deployed hosted Checkout driver or instructions to change the live webhook. |

Future full-success evidence means actual hosted test-card entry for the correct existing subscription, authoritative application completion, the new masked/default method after return and reload, unchanged commercial terms/customer defaults, and no app-initiated charge. The successful replacement stays saved. Cancellation separately preserves the then-current default. Lost responses reconcile the original durable attempt and provider state; no blind second setup or automatic restoration. The small provider-free success helper is prepared and locally verified, but hosted driver/transport integration and provider evidence remain outstanding and paused. No private recovery-store or mandatory restoration prerequisite is imposed.

## Meta / Instagram — owner-controlled setup remains outstanding

This is **implemented code with simulated verification; actual live import is not verified**. It is not a founder-approved deferral. Manual/source-text onboarding remains available at `/salon/onboarding/import`. No scraping, public-profile discovery, publishing, messaging, ongoing synchronization, caption import, inferred prices or other-business data is supported by this adapter.

The implemented source uses Instagram API with Instagram Login, API `v26.0`, permission **`instagram_business_basic`**, and an owner-authorized Business/Creator professional account. It reads the account name/identity and up to 16 eligible owned still images. The actual Girlz Culture Instagram App ID, its approved access state, one acceptance-owner UUID and the final held deployment ID have not been established in the available evidence; a social-profile link is not any of these.

| Service / settings page | Owner-controlled action and exact field/value | Expected signal / dependency |
| --- | --- | --- |
| [Meta App Dashboard](https://developers.facebook.com/apps/) → the Girlz Culture app → Instagram → **API setup with Instagram login** → **3. Set up Instagram business login** → **Business login settings** | Use **Instagram App ID** and **Instagram app secret** from this product setup, not an unrelated Facebook client. Register **OAuth redirect URIs** exactly as listed below; check that Meta has not appended a slash. | The same app's Instagram Login permission/access and authorized professional-account role are available. An App ID or manually generated token alone does not establish access approval. |
| Same Meta app → app roles / Instagram account setup and **App Review → Permissions and Features** | Authorize the real owner-controlled Business/Creator account. Request only `instagram_business_basic`. Confirm the access level and any business/app verification required by Meta for the intended users. | Private acceptance may use approved app-role accounts. General owner access requires the applicable approved access/review; development role access is not general availability. No Facebook Page link or publishing/messages permission is requested by this implementation. |
| Same product's **Business login settings** | **OAuth redirect URIs**: production `https://girlzculture.com/api/salon/onboarding-instagram/callback`. For private acceptance, register the exact `https://<actual 24-character deployment ID>--girlzculture.netlify.app/api/salon/onboarding-instagram/callback`. | Exact origin/path match; no wildcard, query, fragment, custom port or trailing slash. The held ID must come from the selected actual deployment. |
| Meta App Dashboard → app settings / login callback settings | **Deauthorize callback URL**: `https://girlzculture.com/api/salon/onboarding-instagram/deauthorization`. **Data Deletion Request URL** / **User data deletion → Data deletion callback URL**: `https://girlzculture.com/api/salon/onboarding-instagram/data-deletion`. Use the same held origin for private acceptance. | Meta signs callbacks; the app returns an opaque confirmation and `/api/salon/onboarding-instagram/deletion-status?code=<opaque code>`. Exact Instagram callback delivery remains to be verified. Field placement can vary with the configured Meta app product; use its corresponding callback settings, not a Facebook Login replacement flow. |
| Meta App Dashboard → Settings → Basic | **App domains**: `girlzculture.com`; **Privacy Policy URL**: `https://girlzculture.com/privacy`; **Terms of Service URL**: `https://girlzculture.com/terms`. | These are implemented site routes, not a claim that their current content satisfies Meta review. Published privacy/deletion wording must describe the actual retention and owner-confirmed-content behavior below. |
| Netlify → existing `girlzculture` project (site ID `e7da549f-eb32-48e2-9d78-ca06fe2fb91a`) → Project configuration → Environment variables | Enter the exact server variables in the next table, scoped to approved **Production / Functions and Runtime**, including the named held Production-context candidate. Mark secrets sensitive. No `NEXT_PUBLIC_` variables, checked-in `.env` file or broad preview-secret scope. | Correct deployment receives server configuration. Public-origin activation stays unavailable during candidate-only acceptance. No values were added by this handoff. |

The implemented callback is **`/api/salon/onboarding-instagram/callback`**. Do not use the older proposed `/api/salon/onboarding/instagram/callback` from the historical onboarding plan. Likewise, older “no route exists” statements predate the completed adapter; [current acceptance evidence](INSTAGRAM-ONBOARDING-ACCEPTANCE.md) supersedes them.

| Netlify server variable | Exact value or source | When usable |
| --- | --- | --- |
| `INSTAGRAM_ONBOARDING_CLIENT_ID` | Actual numeric **Instagram App ID** from the chosen app. | Same app as its secret, permission and callbacks. |
| `INSTAGRAM_ONBOARDING_CLIENT_SECRET` | Corresponding **Instagram app secret**, entered directly into the secret setting. | Never paste into chat or expose to browser/model/logs. |
| `INSTAGRAM_ONBOARDING_ENCRYPTION_KEY` | A dedicated cryptographically random 32-byte key, standard base64 encoded; keep a secure backup. | The adapter requires exactly 32 decoded bytes. Do not reuse an API secret or rotate while encrypted connections still depend on it. |
| `INSTAGRAM_ONBOARDING_REDIRECT_URI` | Exact registered held-candidate callback above for acceptance; exact production callback above for live. | Must equal the request's origin and implemented callback path. |
| `INSTAGRAM_ONBOARDING_APPROVED` | Leave absent/false until the intended account/access/configuration has been approved; then `true`. | This application gate does not grant Meta permission or replace app review. |
| `INSTAGRAM_ONBOARDING_ACTIVATION` | Leave absent/disabled until ready. `acceptance` restricts access to the named held candidate and owner. `live` is only for completed live-readiness acceptance. | Missing/incomplete settings keep the honest unavailable state and manual onboarding. |
| `INSTAGRAM_ONBOARDING_ACCEPTANCE_OWNER_ID` | Exact existing authenticated business-owner UUID selected for the private acceptance. | Required for `acceptance`; it is not an Instagram account ID. |
| `INSTAGRAM_ONBOARDING_VERIFIED_AT` | Actual past UTC timestamp of completed acceptance, recorded only after it happens. | Required by `live`, together with the production callback. Never prefill a fictional success timestamp. |

Application-side prerequisites already prepared are migration184 (`20260919134856_instagram_onboarding_import.sql`), private `media-originals` staging, exact own-asset checks, the six-section onboarding review, and deletion/retention integration with the existing protected `/api/media/cleanup` job. Release applies reviewed pending schema through the existing migration process; the founder need not manually paste SQL. Existing Netlify `media-cleanup` runs at `15 4 * * *` and uses the existing server-only `CRON_SECRET`; no new scheduler or copied secret is required. Its deployed success/history and private-storage behavior must be verified on the release candidate.

Expected acceptance sequence: owner opens `/salon/onboarding/import` → connects the exact professional account → returns to `?instagram=connected` → explicitly reads source → selects only owned still images → saves a private draft → edits/reloads it → separately reviews all six sections and confirms → authoritative saved business/gallery readback. A successful one-time read erases provider credentials and visibly disconnects while retaining the private snapshot; this is expected. Reading/saving a draft must not attach public gallery photos. Owner disconnect, signed deauthorization/deletion, wrong-business denial and interruption recovery also need actual hosted evidence.

Privacy review must distinguish imported provider data/private staged media from independent owner edits and explicitly confirmed business content. Unused imports/private originals expire after seven days; abandoned reads after an hour become cleanup-eligible. Applied public content is not automatically erased as unrelated owner data, while provider deletion removes exact provenance-bound material and preserves independent edits. This policy and actual callbacks require Meta/privacy acceptance; code and simulated tests alone do not prove compliance.

Existing evidence: 30 backend/cleanup cases, 17 native-image/actual-route cases and 27 manual-onboarding regressions passed locally. Sixteen unique Instagram browser cases have composite production-build evidence (8 unavailable/manual cases on build188 and 8 active fixture cases on build189). Storage/provider responses were simulated; **no actual Meta connection/import or provider-compliance acceptance is claimed**. The current acceptance document records precise logs and limitations.

## Google Business Profile — existing deferral unchanged

**Live activation deferred by founder—awaiting a qualifying salon profile, Google approval and live verification.** No new profile, quota retry, credential request or activation action is requested. Existing project ID `project-f0d9935c-69db-42c9-b22`, project number `886435271334`, has Business Information API enabled but recorded quota zero. Live controls/background sync stay disabled. This is not a current release blocker. The existing [Google access handoff](GOOGLE-BUSINESS-PROFILE-ACCESS.md) retains the future exact callback, server variables, approval prerequisites and acceptance sequence.

## Pending database dependencies and the Stripe pause

The last saved read-only comparison is approximately **20:10 UTC, 19 September 2026**, at source `ff4792fc695628dd57ad4e6c69481e2e651713f2`: **151 applied, 188 local, 37 pending, no remote-only version or name difference; DeepL migration already applied**. Source: `work/redesign-evidence/production-migration-comparison-188.json`. These are **37 total migrations, not 37 Stripe migrations**. The release owner must compare current history again before applying; this handoff applies nothing and does not rewrite migration history.

| Pending migration | Database effect and exact dependency | What remains paused / what does not require Stripe acceptance |
| --- | --- | --- |
| 158 — `20260918072308_business_deposit_rules_and_protected_offers.sql` | Own-business deposit-rule/incident tables and RPCs; immutable booking deposit snapshot and product-offer-scope triggers. Builds on existing bookings/checkout intents/promotions. Existing bookings without a new snapshot remain supported. | Database protections and owner rule records do not call Stripe. Actual new deposit checkout/provider acceptance is separate; do not claim it from SQL installation. |
| 170 — `20260918213511_subscription_recorded_price_terms.sql` | Adds nullable `subscriptions.recurring_price_snapshot` and exact-shape constraint. **No historical price backfill** and no replacement of old commercial agreements. | Column can exist with null evidence. Populating it requires verified existing provider facts; live catalog/agreement verification remains paused. |
| 173 — `20260919015517_subscription_payment_method_update_attempts.sql` | Adds private method-attempt state, owner/association/lease RPCs and minimal service-role grants over existing subscriptions. Creates no provider object. | Schema installation does not complete or initiate hosted method replacement. The provider journey stays paused. |
| 176 — `20260919043012_business_referral_campaigns.sql` | Adds private campaign/code/claim/payment-check/reward/event tables and scoped RPCs linked to the existing canonical billing ledger. Seeds one incomplete **inactive** “Referral campaign draft”; no active campaign, Stripe credit or payout. | Referral configuration/read isolation can work independently; real paid-event qualification/reward-provider acceptance is not established by installing the tables. Do not issue rewards or promise a free month through this handoff. |
| 178 — `20260919052907_subscription_payment_schedule_guards.sql` | Requires173; extends attempts with schedule evidence, introduces private subscription mutation leases/new RPCs and a scheduled-reservation wrapper. A new **BEFORE INSERT reservation trigger** tightens the existing173 reservation path indirectly; it does not replace that RPC. | These are DB-only guards, not a Stripe schedule update. Preserve ordering and app compatibility; live inherited-schedule verification remains paused. |
| 187 — `20260919144513_subscription_payment_explicit_phase_intent.sql` | Requires173+178; adds private schedule-intent archives and immutable phase plans, and tightens `mark_payment_schedule_apply`. Archives are created only by later verified application operations. | No schedule is read or mutated by the migration itself. Real explicit-phase/provider acceptance remains paused; installation does not mean all historical schedules are supported. |

None of these six SQL files requires a Stripe credential or completed live method acceptance **to execute its database statements**. They contain schema/permissions/transactional guards, not provider HTTP calls or newly scheduled Stripe jobs. They are not all “tables only”:158 adds triggers,176 seeds an inactive draft,178 adds a reservation trigger, and187 replaces a callable guard; each advances the migration expectation. Those compatibility effects must remain in the reviewed ordered release/rollback plan. Skipping173 while applying178/187 is invalid; withholding the whole non-Stripe release merely because provider acceptance is paused is also unsupported.

The other31 pending files cover policy/editor capacity, assistant/media/photo/appearance, operating books, scoped history, private clients, service assignments, inventory, client links, reviews/messages, rescheduling/reminders, consent/follow-ups, waitlist, Google, reviewed onboarding, marketing, manual receipts, outstanding balances, customer campaigns, rebooking/contribution, Instagram, profile/settings/money reads and reusable replies. Some **consume already verified financial records** or existing subscription permissions; they do not depend on the unfinished saved-method sandbox journey. Unverified provider receipts remain unavailable rather than invented. Google/Instagram schema installation is likewise distinct from external activation. Existing source/fallback compatibility and hosted acceptance remain release responsibilities, not evidence of live Stripe completion.
