# Product editor and subscription history — 19 September 2026

Status: **bounded browser and visual checks PASS, AUTOMATED ONLY.** These are existing LAYOUT-01/04/06/08 defects, not new product or payment behavior. Nothing in this slice changes provider calls, billing calculations, publication decisions or authorization. This is local fixture acceptance of the changed editor/history views, not full dashboard or hosted acceptance.

## Reproduced before changing application source

Two focused Chromium cases ran against the existing local production fixture build. The new-product editor placed the required Name field after the photo label: Name's bottom was543px, photo label's top141px. The subscription history region was356px wide but its content required901px. Exact assertions and original logs remain outside Git under `../redesign-evidence/product-editor-before.log` and `product-history-before.log`, with corresponding screenshot/trace folders. The first product attempt used an incorrect spinbutton locator; its snapshot showed the actual textbox role. That locator alone was corrected, and the one product case was rerun before any application edit. The subscription failure was already a genuine geometry failure.

## Correction

- Product editor: Name and regular price are first, followed by optional SKU/sale price and description. A native Photos disclosure preserves one mounted uploader for a saved record; unsaved new records show the existing translated save-first notice instead of a large disabled uploader and technical paragraphs. The redundant inner heading is desktop-only. Existing submit fields, upload attachment/handlers, inventory, shipping, publication and archive controls remain intact.
- Subscription history: below1024px each event is a labeled card retaining date, event, plan, collected/refunded/credited amounts, payment status, failure reason and full invoice/event reference. Desktop keeps the existing table with explicit column-header scopes. Empty history is a wrapping message outside the desktop-width table. Payment-method and lifecycle controls are unchanged.

## Verification

- TypeScript and targeted ESLint: **PASS** (`product-history-types.log`, `product-history-lint.log`).
- Independent source review by assistant_operational: **PASS**, including named form fields, uploader lifetime/attachment and complete billing evidence; requested table header scope was added.
- New browser cases: **20/20 PASS**, five product cases and five subscription-history cases per engine. Product cases cover EN390px, FR768px, ES1440px, ZH844px landscape and permitted staff; required-fields geometry, disclosure draft retention, failed save/explicit retry, saved-record reload, upload availability and one uploader. History cases cover the same four layouts/languages plus unauthorized staff; all recorded values, long reference, empty state, no horizontal clipping and no mutation.
- Existing affected coverage: **16/16 PASS**, four real-stock/filter/back/reload product cases and four recorded-price subscription cases per engine. The combined result is **36 unique passing cases**, not the sum of every interrupted attempt: four existing Chromium Products passes from `combined-183-advice-products-browser.log`, 27 other Products/Subscription passes from `combined-183-final-advice-products-browser.log`, and the five remaining WebKit Subscription passes from `combined-183-final-subscription-browser.log` (22.4 seconds). All logs and captures remain outside Git in sibling redesign-evidence. Unchanged feature-source evidence was reused; no full suite or unchanged remote rerun was started for this correction.
- Final shared production build digest: `bc3a57809a3a5a60141e27ffe0438f83f6b22e3dc8e8d3da625e4cca55f0d200` across 803 application/assets/config files. The final five cases used this same frozen build; their correction was test-only.
- Root visually inspected the current Chromium390 Product required-fields view and Subscription cards, plus the WebKit390 Subscription before-reload capture. Required product fields are immediately readable; billing cards retain every field and the complete wrapped reference. Representative captures are linked in `VISUAL-ACCEPTANCE-MATRIX-2026-09-19.md`. These are bounded editor/history views, not a complete Subscription page or all14-section visual acceptance.

## Preserved after-build test failures and corrections

The first new Product case passed the required-fields geometry but its disclosure locator failed: an editor-rooted locator nested inside `details.filter({has: ...})` required a second dialog inside the disclosure. Both new/saved disclosure filters now use the page-relative exact Photos label. The original geometry, draft, save, upload and persistence assertions remain. The original failure is preserved in `combined-183-advice-products-browser.log` and its results folder.

The later combined run stopped at 61 passes, one failure and four unrun cases. WebKit390 Subscription rendered all billing evidence correctly, then reload lost fixture interception. Its original trace records `serviceWorkers: allow`, an initial mocked workspace GET at11:40:10.437UTC (`200`, `_wasFulfilled: true`), production `/sw.js` registration at11:40:10.439, and an unmocked workspace GET after reload at11:40:12.150. The latter reached the real local server and returned503 after fixture-provider authentication rejected the unexpected request. The rendered support reference was `35de42e1-d904-4e97-9ced-13c73c2e0aa6`; no fixture fulfill call occurred after reload. This was not evidence of a live authentication regression.

The new owner-fixture spec had omitted the `test.use({serviceWorkers:'block'})` setting used by the other page-route-based owner tests and recommended by the installed Playwright API documentation. Adding that setting kept the fixture authoritative across reload. Application, build and authentication code were unchanged. All billing values, reload-to-empty, forbidden-staff, no-mutation and unexpected-request assertions remain. No sleeps, automatic test retries or permissive fixture fallback were added. Only the failed case and four previously unrun WebKit cases were run after this correction; all five passed. The original log/trace/screenshots remain in `combined-183-final-advice-products-results`, with diagnosis in `subscription-history-webkit-fixture-correction.md`.

Local fixture screenshots and passing checks cannot establish live authorization, provider behavior, hosted persistence or production deployment. Updated-source protected CI and release acceptance remain separate requirements.
