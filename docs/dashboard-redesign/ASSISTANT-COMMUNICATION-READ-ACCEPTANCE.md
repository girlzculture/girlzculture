# Assistant conversation, review and customer lookup evidence

Status: **AUTOMATED ONLY**. This is a bounded PAGE-09/PAGE-10/DATA-01 correction; no hosted or live-model acceptance is claimed.

The founder handoff's Messages section asks for contextual assistant help; the Reviews section asks for genuine review analysis and assistant help. Existing page actions already offered conversation summaries/reply drafts and review themes. The three existing read tools retrieved records, but the planning projection replaced their results with `null`. All three answer paths then rejected the result as `ASSISTANT_INVALID_PLAN`.

## Change

- `get_booking_messages`, `get_reviews` and `get_customers` now supply dedicated, bounded projections to both planning and answering. A message uses its saved original text, with the legacy original-body fallback only when necessary. Review moderation and original response text remain distinct. Customer lookup keeps one association per booking; it is not a distinct-client count and does not merge names.
- Exact totals, shown counts, partial-page indicators and text-excerpt indicators accompany up to 12 projected rows (latest first for messages/reviews). Message/review bodies and record names are untrusted quoted data. The existing common contact redaction still applies. Only strict UUID record/booking selection keys from these freshly reauthorized reads retain their exact value after redaction; customer account IDs, contacts, translations and private notes are excluded.
- The source readers recheck current business ownership or active membership/assignment and the existing section permission before and after reads. Business/booking bindings and provider row counts are checked; foreign or mixed rows fail closed. Assigned booking IDs are checked again before returning. Reviews now refresh on every follow-up, including `previousRequestIds` without transcript IDs.
- Answer-only terminology guidance selects one domain from literal authorized tool mappings, or a literal page mapping for a single business summary. Original record text takes precedence. EN/FR/ES/zh-CN use the reviewed module; Wolof, planning and mixed-domain answers do not claim glossary coverage. No DeepL context, model, pricing, permission, payment, notification or $25 cap changed. Guidance remains included in the existing input-size/cost reservation.

## Reproductions and focused evidence

- [Original projection failure](../../../redesign-evidence/assistant-conversation-projection-before.log): **0/6 passed**; three null planning results and three rejected answer phases. The separate `*-before-runner.log` records a sandbox process-spawn failure; the actual reproduction used Node's in-process test isolation and simulated provider transport.
- [Original source-scope boundary](../../../redesign-evidence/assistant-communication-scope-before.log): **0/10 passed**, including missing rejection after permission/reassignment changes and foreign provider rows. [Initial corrected boundary](../../../redesign-evidence/assistant-communication-scope-after.log): **10/10 passed**. The final suite additionally covers both businesses and 1,001 same-name booking associations without deduplication.
- [Terminology before](../../../redesign-evidence/assistant-terminology-before.log): four required-locale guidance checks failed; omission checks passed. [Corrected terminology and existing operational cases](../../../redesign-evidence/assistant-communication-terminology-final.log): **12/12 passed**. Original names, UUIDs, monetary amounts, dates and currency remain unchanged in captured model payloads.
- [Partial excerpt reproduction](../../../redesign-evidence/assistant-communication-excerpt-before.log): the one-of-two row case failed before its marker correction.
- The initial broader run is preserved in [assistant-communication-integrated.log](../../../redesign-evidence/assistant-communication-integrated.log): **87 passed, 7 failed** because its existing fixtures had no complete current owner/member/timezone source for the new checks. Fixtures now provide those real-shape records; original business, permission, financial and pagination assertions are unchanged.
- [Final focused integration](../../../redesign-evidence/assistant-communication-integrated-final.log): **100/100 passed**, zero failed/skipped, 129.4 seconds. This includes the source-scope tests, complete planning suite and affected operational suite. [Whole-tree TypeScript](../../../redesign-evidence/assistant-communication-types-final.log) and [focused ESLint](../../../redesign-evidence/assistant-communication-lint-final.log) both exited zero.

## Limits

This uses actual readers, dispatcher/planner serialization and source guards with simulated database/model transports. No provider request, customer message, payment, database migration or production change occurred. The frozen build197 browser results precede this source change and do not verify it. The next compiled build, protected release gates and authorized hosted assistant acceptance remain outstanding. This does not add an attachment workflow or claim complete conversation/review analysis when only excerpts are available.
