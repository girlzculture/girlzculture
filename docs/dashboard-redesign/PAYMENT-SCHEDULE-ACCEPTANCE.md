# Scheduled payment-method support

Migration187 adds a bounded explicit-phase implementation to the inherited-default path below. The earlier blanket external-single-writer prerequisite was an engineering interpretation, not a founder requirement. The implemented controls are the existing shared app mutation guard, an exact retained commercial baseline, fresh preflight, durable idempotent stages and authoritative readback. Stripe exposes no documented compare-and-swap: these controls do not prevent an uncoordinated external Dashboard/API writer from racing a full phase update. External operators must coordinate normal billing work; the application never automatically restores an older agreement. Provider acceptance remains pending.

Explicit phases require a complete recognized `2025-06-30.basil` snapshot and provable original empty-discount inheritance/removal intent. Ordinary app downgrades now pin only their schedule phase write/readback to that version and use `iterations:1` for the already verified monthly price. Their successful full response and actual outgoing intent are archived privately, surviving lease reuse. A later method-only override can use that archive when all commercial settings still match. Legacy lease evidence is adopted only if its complete phase request exactly matches the recognized snapshot; sparse historical duration requests, unknown fields, stale intent, legacy sources and one-off invoice-item phases remain review-required. No universal legacy-schedule support is claimed.

The explicit path freezes the exact payload, source/expected fingerprints and target before first send. It replaces current/future explicit methods, retains inherited phase nulls and past phases, and binds the schedule default to the new method without changing customer-level defaults. All other recognized phase fields are resubmitted unchanged with update-time proration `none`. The subscription default remains a separate stage if needed. Same-target completion is read-only when both schedule and current subscription already match; inherited defaults still receive the necessary binding. Phase transition proximity, observed commercial drift, expired retry windows and uncertain readbacks cannot report success. Archival failure after a provider write retains the hold and cannot trigger compensating release.

Latest local evidence: **128/128 Node tests passed, zero failures/skips**, covering the method, phase serializer, explicit server path, shared guard and actual downgrade route. The ordinary downgrade archive is exercised through the real route, and wire-level tests verify both pinned POST and GET headers. Original409 and same-target failures remain in `payment-explicit-before.log` and `payment-explicit-same-target-before.log`; the ordinary bridge regression is recorded separately. Migration187 applied to the disposable local183→186 database and its actual-service-role verifier passed with ROLLBACK. One ambiguous SQL variable was corrected via a controlled local function refresh before the passing verifier. Logs: `payment-explicit-final-node.log`, `payment-explicit-187-{apply,refresh,sql}.log`. The final SQL SHA256 is `C2A891E81198AFB5CEB7B9F95BAE98C666B3AF41119141F301EA843327F635D6`. Independent review of the final ordinary-producer/version bridge passed; strict scoped TypeScript and focused lint passed. No provider or production action occurred. Final combined build, required CI, sandbox transitions/invoices and hosted acceptance are pending. The following evidence table retains earlier178 history.

Status: **IN PROGRESS / AUTOMATED ONLY / NOT RELEASED**. No real Stripe request, payment, invoice, schedule mutation or production migration was performed for this work.

The original server rejected every attached subscription schedule before opening setup. A simulated inherited schedule reproduced that409; the preserved log is `redesign-evidence/payment-schedule-before.log` outside Git.

The original178 inherited subset is an active schedule bound to the authenticated business's exact stored subscription/customer and Stripe mode. Its current and future phases explicitly return null default methods, have complete sequential dates and recognized item/phase fields, and contain no legacy source override. Migration187 extends this only as described above.

Setup retains the existing Stripe customer, setup-only Checkout and immutable attempt generation. Migration178 adds the original schedule object and canonical schedule/subscription fingerprints, a separate schedule-stage first-send time, verification time and safe request ID. The only schedule write parameter is `default_settings[default_payment_method]`. No phase, price, quantity, date, tax, discount, cancellation, release, invoice or proration value is sent. If the current subscription does not inherit immediately, its existing default-only update runs as a separate durable stage with a separate idempotency key/time. Success requires both authoritative objects to resolve to the target and all captured commercial values to remain unchanged.

Lost delivery is reconciled by reads first. A target already applied is not sent again. Unresolved stages may retry only the same stable key/target within23hours of their own persisted first-send time. An older stage can finish only by read-only reconciliation; external defaults, changed phases or changed associations stop the operation. No automatic restoration of an old provider snapshot occurs.

Plan-change and lifecycle routes now share an owner/subscription-bound mutation guard with method reservations. Provider calls have one45second deadline, and the database lease has a two-minute envelope with a one-minute remaining-time pre-write check. Both directions use the same transaction mutex and active-attempt exclusion. A provider-write latch survives lease expiry when delivery is uncertain. A validation failure before any provider write releases its lease; only an allowlisted parameter-rejection400 plus unchanged authoritative baseline reads can clear a first rejected write. Payment-required402, idempotency409, network failures and5xx do not establish no mutation.

Ambiguous multi-step plan/lifecycle results require billing reconciliation. Their exact frozen request parameters, stable keys, baseline hashes, first-send times and allowlisted provider request IDs remain in a private service-only relation. Browser APIs return a specific409 and the exact protected Engine incident reference. No automatic retry of an uncertain commercial operation or administrative hold-clearing endpoint is introduced. The same verified in-flight operation can finish normally; a different generation cannot release its hold. This limitation is explicit and must be included in release review.

## Evidence

| Check | Status | Result |
| --- | --- | --- |
| Payment method and schedule server/protocol regressions | AUTOMATED ONLY |61 Node tests passed,0 failed,0 skipped. Setup-only, inherited/current defaults, nested commercial preservation, schema/owner/mode/association rejection, phase transition/drift, unchanged key order, duplicate return/webhook, no replay after completion, uncertain delivery before/after each stage, independent clocks and23hour expiry. |
| Shared guard and route incident contract | AUTOMATED ONLY |22 Node tests passed,0 failed,0 skipped. Active setup blocks provider access, every write rechecks its lease, one deadline, uncertainty hold, validation/rejection distinction, authoritative baseline verification and both lifecycle/plan JSON reference correlation. A failed read-only invoice-preview hold was reproduced before splitting lease assertion from write marking; previews now release normally. Actual-route regressions reproduced same-period cancel/reactivate key reuse and false success from stale POST responses. Lifecycle stages now use durable intent-specific keys and authoritative GET readback before local save. Schedule release verifies the released subscription and preserves an existing cancellation. The actual Subscription action previously discarded a protected incident reference; it now preserves validated UUIDs for the existing toast, with FR/ES/zh source-catalog coverage. The new exact protected-reference case passed in both Chromium and WebKit against the combined179 build. |
| Repeated downgrade generation | AUTOMATED ONLY |4 actual-route tests passed after reproducing downgrade→cancel→same downgrade returning cached success for a released schedule. Create/phases/cleanup and local request-record keys now belong to the durable intent. Fresh schedule fields and unchanged current subscription are verified before local success; failed or mismatched readback retains the uncertainty hold and does not release an unverified agreement. Existing upgrade preview lookup remains unchanged. |
| Real PostgreSQL authorization and durable stages | AUTOMATED ONLY | Original173 and current178 verifiers passed in the isolated schedule fixture. A fresh `girlzculture_combined_178_release` clone of verified173 then applied174–178 sequentially and passed all five new SQL verifiers, including actual service_role, owner transfer, foreign generation, immutable stage times, unchanged commercial rows and browser grants. Before/after source hashes match; Engine marker is20260919052907. All verification fixtures rolled back. This does not substitute for CI's full-from-zero chain or protected production application. |
| Independent source review | AUTOMATED ONLY | A separate reviewer found no additional blocker in the final helper, server, migration, shared guard and route integration. The review covered exact default-only payloads, independent immutable stage times, recovery limits, identity/fingerprints, shared locks and uncertain-write holds; live provider semantics remain unverified. |
| Actual concurrent transactions | AUTOMATED ONLY | Two real PostgreSQL connections exercised lifecycle-first and method-first. The contender was observed waiting on the shared database lock and then rejected the committed conflicting action. Log `redesign-evidence/payment-schedule-concurrency.log`; no timed sleep substitutes for an assertion. |
| TypeScript and focused lint | AUTOMATED ONLY | Passed after the source corrections. No speculative dependency change. |
| Required CI / final production build | BLOCKED | Must cover the eventual combined PR commit. Focused Node and SQL verifiers are wired into the existing protected validation paths. |
| Real Stripe sandbox and transition acceptance | BLOCKED | Still requires authorized sandbox identity and controlled setup/return/cancel, current/future default and sibling-field readback, then phase-transition/subsequent-invoice verification. Simulated writes cannot prove Stripe's deployed API-version semantics. |
| Explicit phase overrides | AUTOMATED ONLY / BOUNDED | Migration187 implements recognized archived-intent schedules, immutable full payload, fresh preflight/readback and recovery. Unknown or ambiguous legacy intent and one-off invoice items remain review-required; sandbox behavior is unverified. |

Stripe's [schedule guide](https://docs.stripe.com/billing/subscriptions/subscription-schedules) documents inheritance and requires complete retained phase settings. The inherited path avoids phases; the explicit path uses the [pinned contract](https://github.com/stripe/stripe-node/blob/v18.3.0/types/SubscriptionSchedulesResource.d.ts) and verifies the full expected snapshot. Application locks do not serialize an external Stripe Dashboard/API operator. Observed drift fails closed; neither hashes nor readback are represented as a provider precondition.
